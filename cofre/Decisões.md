---
tags: [cofre, decisoes]
atualizado: 2026-06-23
---

# Decisões

Log de decisões de produto e técnicas (mais recentes no topo).

## 2026-06
- **Formulário de suporte na página Suporte do aluno → e-mail direto p/ o suporte (2026-06-23, pedido do usuário):** a página Suporte tinha só FAQ + um `mailto:`; o usuário pediu um **formulário** que, ao enviar, **manda o e-mail automaticamente** para o nosso suporte. **Frontend (`index.html`, `panel-support`):** form com Nome, E-mail (pré-preenchidos do `currentUser`), Assunto (Dúvida/Problema técnico/Sugestão/Financeiro/Outro) e Mensagem + botão; `initSupportForm` (bind no `goPanel('support')`) e `submitSupport` (POST `/api/ai` `{kind:'support',...}`); mostra "Mensagem enviada!" no sucesso; `mailto` mantido como alternativa. **Backend:** `lib/support.js` (módulo — **não conta como função**) valida (e-mail + mensagem) e envia por **Resend** para `SUPPORT_TO`→`contato@endodirect.com.br`, com **`reply_to` = e-mail do aluno** (equipe responde direto). Roteado por um branch **`kind:'support'`** dentro de `api/ai.js` (reusa a **proteção same-origin** do endpoint; tratado **antes** da checagem de `ANTHROPIC_API_KEY` pois usa Resend, não a IA) — escolha feita para **respeitar o teto de 12/12 funções serverless** do plano (sem criar endpoint novo). **Verificação:** CI verde (12/12 mantido); teste de lógica (validação, sucesso/erro do Resend, roteamento, bloqueio de origem externa, funciona sem a chave da IA). **Env p/ produção:** já usa o `RESEND_API_KEY` existente; opcional `SUPPORT_TO` p/ trocar o destino. Frontend+backend → **preview + aprovação** antes do deploy. Ver [[Integrações]].
- **Questão do Dia vira JANELA (modal) na frente do mural + correções do mural (2026-06-23, testes do usuário no preview do #423):** o usuário testou o preview, aprovou os geradores de IA e pediu 3 ajustes (depois: "pode deployar tudo"). **(1) Questão do Dia = modal:** em vez de card no Dashboard + topo do Mural, a questão aprovada mais recente aparece como **janela na frente do mural** (backdrop esmaece o mural). O aluno responde → vê **acerto/erro + explicação comentada** + botão **"Ver o mural →"**; ao fechar (botão, "Agora não" ou clicar no backdrop), o mural volta ao normal. Só aparece **no Mural**, 1× por sessão ou até responder (`localStorage qotd_answered` + `qotdDismissed`). Implementado com `qotdModalEl`/`qotdRenderModal`/`qotdClose`/`renderQotd` (modal criado em runtime e anexado ao `body`; CSS `.qotd-overlay/.qotd-backdrop/.qotd-dialog`, z-index 9000). `goPanel` mostra no mural e **esconde** o modal nos outros painéis; `refreshDash` não chama mais o qotd. Os divs antigos (`#qotd-card`, `#qotd-mural-wrap`) ficaram **inertes** (display:none). Resolve também a pergunta de posicionamento (modal no mural, sem dashboard). **(2) Mural "desatualizado":** o `8f116a6` (ordem por tipo de estudo) trocou o default de `'novos'` para `'relevantes'`, enterrando as notícias novas sob os fixados → o usuário viu "desatualizado". Default volta a **`'novos'`** (mais novos primeiro) no aluno (`muralSort`) e no professor (`admMuralSort`); **`'relevantes'` continua no seletor** (a função de ranking por tipo segue disponível). **(3) Breaking News quebrado no iPhone:** URLs longas (ex.: `http://www.fda.gov/news-events/...`) dentro de `.mural-text`/`.mural-source` não quebravam → overflow horizontal cortando o texto. Fix CSS: `overflow-wrap:anywhere;word-break:break-word` em `.mural-text`/`.mural-source`/`.mural-card h3` + grade mobile `minmax(0,1fr)`. **Verificação (sem browser):** CI verde; simulação de lógica do modal (abrir→responder→gabarito→fechar→mural some/normal) OK. Aprovado p/ deploy ("pode deployar tudo") → merge do #423. Ver [[Instagram Stories (Questão do Dia)]] e [[Newsletter e Radar]].
- **Prompt caching das DIRETRIZES + diretrizes INTEIRAS para todo gerador de IA (2026-06-23, "Pode utilizar o recomendado") — CONSTRUÍDO (na branch `claude/brave-mayer-8x0lna`/PR #423, preview):** resolve a pendência do #422 (as diretrizes ainda eram cortadas no slice de 8000). **O quê:** `CLINICAL_GUIDELINES` (~38,6k chars ≈ **~10,7k tokens**) agora vai **INTEIRO** em TODO gerador (questões, flashcards, mapas, Chat IA, OSCE, Prescrição) e é marcado como **prefixo cacheável** (`cache_control:{type:'ephemeral'}`, GA — sem header beta). **Como (servidor `api/ai.js`):** o `system` deixou de ser `slice(0,8000)` e virou **string OU array de blocos `text`** — (a) se contém o **sentinela** `__ENDODIRECT_SYS_SPLIT_b1f7__`, separa o **prefixo ESTÁVEL** (diretrizes, cacheado) da **cauda VARIÁVEL** (regras de autoria + formato JSON, não cacheada); (b) `system` longo e FIXO **sem** sentinela (Chat IA/OSCE/Prescrição, que embutem as diretrizes no fim) é cacheado **inteiro** (limiar ≥20.000 chars → bem acima do mínimo cacheável: Opus 4.8 4096 / Sonnet 4.6 2048 tokens); (c) curtos seguem string simples. Slice subiu p/ **60.000** (cabe as diretrizes). **Cliente (`authoringSys`):** passou de `fmt + AUTHORING + GUIDELINES` (hotfix #422, fmt na frente p/ sobreviver ao corte) para **`GUIDELINES + SENTINELA + AUTHORING + fmt`** (diretrizes na frente = prefixo idêntico em todos os geradores → **mesmo bloco de cache**). O sentinela é **byte a byte igual** nos dois arquivos. **Efeito/custo:** 1ª chamada grava o cache (**1,25×**), as seguintes leem a **~0,1×** → a IA passa a ver as diretrizes **completas** a custo controlado. Cache é **por modelo** (entradas Sonnet e Opus separadas) e por 5 min (TTL ephemeral). **Verificação:** teste de lógica do split (prefixo idêntico p/ fmts diferentes; cauda varia; nada truncado) + `api/ai.js` agora devolve `usage` na resposta (confere `cache_read_input_tokens` no devtools/Network, sem depender de logs da Vercel). **Risco baixo:** os 6 fluxos interativos (chat/OSCE/prescrição) **não** foram reordenados — o servidor cacheia o bloco fixo inteiro; pior caso = cache miss = custo de hoje. **Defesa extra (pedido do usuário "nada pode quebrar como hoje"):** o `authoringSys` passou a **encerrar** com "RESPONDA SOMENTE com JSON válido…" (o formato fica colado ao turno do usuário, mesmo vindo depois de ~10,7k tokens de diretrizes). **Verificação executada (sem chave/preview — egress p/ vercel.app é bloqueado):** rodei o **handler REAL de `api/ai.js`** com `fetch` mockado e as **funções-cliente reais** (extraídas do `index.html`) p/ os **8 geradores** (questões MC/dissertativa, flashcards, mapas, Questão do Dia/Opus, Chat IA, OSCE, importação curta) — todos: requisição válida p/ a API, **diretrizes inteiras**, **formato JSON íntegro**, sentinela não vaza, sem null bytes. Testei também o **parser** (`parseJ`/`pickQuestions`) com JSON limpo, em cerca markdown e cercado de prosa → tolerante. **Guarda permanente no CI:** novo `scripts/test-ai-system.js` (rodado pelo `ci-validate.js`) falha o build se o system voltar a truncar/perder o formato ou se o sentinela dessincronizar client/servidor. `api/ai.js` também devolve `usage` (confere `cache_read_input_tokens` no devtools). **Risco baixo:** os 6 fluxos interativos não foram reordenados (servidor cacheia o bloco fixo inteiro; pior caso = cache miss = custo de hoje). CI verde (12/12 funções; 6 scripts inline; regressão IA/system). Frontend+backend → **preview + aprovação** antes do deploy. Ver [[Instagram Stories (Questão do Dia)]] e a skill `claude-api`.
- **🚨 Hotfix DEPLOYADO — gerador de questões por IA quebrado: o system era cortado em 8000 chars e descartava o formato JSON (2026-06-23, #422):** aluno não conseguia gerar questões ("Resposta inválida" / depois "demorou demais" / "não retornou questões"). **Causa-raiz (medida):** `api/ai.js` faz `system.slice(0,8000)` e o bloco `CLINICAL_AUTHORING+CLINICAL_GUIDELINES` cresceu p/ **~40.000 chars** → como `authoringSys(fmt)` concatenava o **formato JSON no FIM**, ele era 100% cortado → a IA respondia fora do JSON → o parser não achava questões. **Afetava TODOS os geradores** que usam `authoringSys` (questões, flashcards, mapas, OSCE, stories). **Fix:** `authoringSys` agora põe o **fmt no INÍCIO** (`fmt + AUTHORING + GUIDELINES`) → o formato sempre sobrevive ao corte; reforço no prompt do `genQuestions` ("responda só JSON"); geração em **lotes paralelos** (≤3/lote, dedup) que também corrige o timeout de 60s; modelo fixado em **Sonnet**. (Iterações até a raiz: truncamento→timeout→429-aparente→**formato**.) **Deploy ISOLADO** (PR #422, cherry-pick sobre a `main`, sem a feature da Questão do Dia) → produção READY (`dpl_6YxB…`, commit `ee14065`). **PENDÊNCIA RESOLVIDA (2026-06-23):** as diretrizes não são mais truncadas — implementado o **prompt caching** (ver entrada no topo) que manda `CLINICAL_GUIDELINES` inteiro a ~0,1× após a 1ª chamada. Ver [[Pendências]].
- **Questão do Dia no app + funil "resposta no app" + lote (2026-06-23, "vamos partir pra cima") — CONSTRUÍDO (PR #423, preview):** card clicável da Questão do Dia no **Dashboard e no topo do Mural** (o aluno responde na plataforma — a "enquete" da casa); **migração ADITIVA** expondo `ig_stories` em `endodirect_member_content`/`endodirect_public_content` (questão **livre** = isca do funil, sem tocar no gating); **calendário Seg–Sáb=questão / Dom=2 promos** (ajuste do usuário, sem "dia de gabarito"); **legenda com CTA "resposta no app"**; **geração em lote** no painel (N questões, variar subespecialidades, concorrência 3, revisar/aprovar/aprovar-todas). Reaproveita o `authoringSys` já corrigido (#422). Frontend → preview + aprovação. Ver [[Instagram Stories (Questão do Dia)]].
- **Mural — ordem por TIPO de estudo como critério principal (2026-06-23, pedido do usuário):** o usuário pediu que o mural **sempre** mostre os artigos nesta sequência de importância: **Artigo de Revisão > Revisão Sistemática > Metanálise > Ensaio Clínico > Coorte > Caso-controle > Transversal > demais.** Implementado em `index.html`: nova função **`muralStudyRank(a)`** (detecta os subtipos finos no tipo+título+texto via `muralPlain`, mesmo quando a normalização os agrupa num rótulo grosso — ex.: "systematic review" cai em 'Metanálise' no rótulo, mas o rank o reconhece como sistemática) e **`muralRelevance`** reescrita para o **TIPO dominar** (a revista virou só leve desempate; a data desempata por último em `muralSortList`). Avisos do professor (Urgente/Breaking/Aviso/Comunicado/Evento) seguem **no topo**. O **default do mural** (aluno `muralSort` e professor `admMuralSort`) passou de `'novos'` para **`'relevantes'`** para a ordem aparecer por padrão (o seletor "Mais novos/antigos" continua disponível). **A confirmar com o usuário:** Diretriz/Perspectiva ficaram **no tier 0** (junto de Artigo de Revisão). A **newsletter** (`lib/newsletter.js`, ordena por FRESCOR primeiro) **NÃO** foi alterada — só o mural. Frontend → preview + aprovação. Ver [[Newsletter e Radar]].
- **Stories "Questão do Dia" — MVP CONSTRUÍDO (2026-06-23, "Pode construir"):** implementado o MVP da rota C (híbrido com aprovação). **Restrições respeitadas:** `api/` em **12/12 funções** e **2/2 crons** (teto do plano) → NÃO criei função nem cron; o recurso é **módulo `lib/instagram.js` + seção no painel + carona no cron do radar**. E **não há `package.json`/deps** no deploy (serverless = Node puro + `fetch`) → o `@resvg/resvg-js` do sandbox NÃO existe em produção → a **arte é renderizada 100% no navegador** (SVG→canvas→PNG 1080×1920; logo dourado desenhado no canvas, same-origin = sem taint). **Entregue:** (1) **`lib/instagram.js`** — calendário por dia da semana BRT (Seg/Qua/Sex=questão · Ter/Qui/Sáb=gabarito · Dom=2 promos), legendas e **lembrete diário por e-mail** (Resend, destinatários = admins/ALERT_TO como o `alert.js`), idempotente por dia (`ig_notice_sent`, preservado como os `newsletter_*`). (2) **Carona no cron** `api/cron/endocrine-radar.js` (`sendIgDailyNotice`, fail-safe, após trial-emails). (3) **Painel → "📲 Questão do Dia"** (`admStoriesHTML`/`bindAdmStories`): gerar questão de **4 alternativas** com a IA (`aiRequest`+`authoringSys`), editar, **pré-visualizar**, **baixar PNG** (pergunta+gabarito), **copiar legenda**, **aprovar p/ a fila**, gerir a fila (editar/baixar/marcar postada/excluir) e **promos de domingo** (fundo branded por ferramenta + legenda). (4) **Fila `ig_stories`** no estado global (professor escreve via `globalStatePayload`; o cron só LÊ). CI verde (12/12 funções; 6 scripts inline OK). **Pendente (futuro):** publicação automática via Graph API; texto justificado na arte; imagem do caso embutida (hoje sairia por taint cross-origin → adicionar no editor do IG); prints reais nos promos. Frontend → preview + aprovação. Ver [[Instagram Stories (Questão do Dia)]].
- **LME no Memed não aparece NEM na plataforma do próprio Memed (glargina) → é do lado deles; acionar suporte antes do plano B (2026-06-23, teste do usuário):** seguindo a decisão de 2026-06-22 ("LME é nativo do Memed; era passo de uso"), o usuário testou emitir LME de **insulina glargina** **direto no site/app do Memed** (iPhone, sem passar pelo EndoDireto) e a opção **"Retirar no SUS / Preencher formulário LME"** **não apareceu** mesmo assim. Isso **refina** a decisão anterior: não é só "passo de uso" — se a opção some na própria plataforma do Memed, o problema é **config/catálogo do lado da Memed**, provavelmente (a) **módulo de alto custo/LME não habilitado na conta** do médico ou (b) **glargina sem a etiqueta "LME" no catálogo** da Memed (glargina é dispensada pelo Componente Especializado/alto custo do SUS, então *deveria* ser elegível). **Decisão do usuário (AskUserQuestion): "Só acionar o Memed antes"** — o usuário aciona o **suporte da Memed** primeiro (mensagem pronta no chat: confirmar se o módulo LME/alto-custo está habilitado na conta e se a glargina tem etiqueta LME no catálogo; anexar print). **Plano B em STANDBY:** expor o nosso `prescPrintLME` (laudo CEAF próprio) **ao lado** do Memed — **não está na branch** (a `claude/brave-mayer-8x0lna` foi **resetada à `main`**, sem código pendente), mas dá p/ reativar em ~5 min (preview + aprovação) se o Memed não resolver. **Nenhuma mudança de código agora.** Ver [[Integrações]], [[Pendências]].
- **🚨 Hotfix de pagamento DEPLOYADO — pagar.me v5 exige `item.code` (2026-06-23, aprovação do usuário "pode dar deploy"):** cliente não conseguia assinar (Sócio-fundador / Gold anual) — o pagar.me recusava com **"The item Code is required. [status: failed]"** em qualquer cartão, com ou sem cupom. **Causa:** o pagar.me v5 passou a exigir o campo **`code`** em cada item do pedido/assinatura; `api/checkout/order.js` (anual) e `api/checkout/subscribe.js` (mensal) montavam o item **sem `code`** → o pedido era rejeitado **antes** da cobrança (falha imediata). **Fix (1 campo por arquivo):** `code: 'endo-'+planKey+'-anual'` (order) e `'-mensal'` (subscribe); não mexe em valores, escopos nem liberação de acesso. **Deploy ISOLADO (lição):** a branch `claude/brave-mayer-8x0lna` (PR #419) tinha ficado **stale** depois que a `main` avançou (#417 e-mails de degustação, #418 Mural) → o diff do #419 **reincluía a feature do Mural** (já em produção via #418), o que tornaria o merge redundante e **arriscado** (frontend não re-testado no browser, no meio de um hotfix de pagamento). Em vez de mergear o #419 inteiro, fiz **cherry-pick do commit limpo de pagamento** sobre a `main` atual → **PR #420** (só os 2 arquivos de checkout) → CI `validate` verde → squash-merge → **deploy de produção READY** (`dpl_C4NA…`, commit `32d2463`). **#419 fechado** como redundante. **Regra reforçada:** quando uma branch de PR acumulou vários trabalhos **e a `main` andou**, num hotfix urgente **não mergear o PR inteiro** — **isolar** a correção por cherry-pick sobre a `main` atual (PR novo, mínimo). Antes de mergear qualquer PR de **pagamento**, revisar o diff completo (não confiar no título/descrição) — foi o que pegou o escopo inflado do #419.
- **Stories "Questão do dia": pode usar questão do TEEM, com atribuição "(TEEM ANO)" (2026-06-22, pedido do usuário):** refina a decisão anterior (que era "só casos originais, não repostar TEEM verbatim"): **eventualmente** uma questão do **próprio TEEM** pode ir ao ar; quando for, o enunciado deve **começar identificando a origem** no formato **"(TEEM ANO)"** — ex.: *"(TEEM 2026) Homem, 65 anos…"*. ⚠️ Caveat de direito autoral (prova oficial SBEM) mantido: atribuição ajuda, mas preferir paráfrase/confirmar permissão para enunciado verbatim. A feature de Stories ainda **NÃO está construída** — isto é guia de conteúdo para quando for. Ver [[Instagram Stories (Questão do Dia)]].
- **Mural: gerar texto do aviso por IA (link ou arquivo) + abas de subtema/botão "voltar" mais visíveis (2026-06-22, pedido do usuário, 2 prints) — DEPLOYADO em produção via #418:** **(1) Contraste:** as abas de subtema (`.cat-pill`) e o botão **"← Todas as subespecialidades"** (`.dir-back`) usavam `background:var(--s2)` (#1c2a48), quase igual ao card no tema escuro → apagados (apesar do ajuste do #415/#416). Trocados para **chip claro "branco"** no dark (bg #e8eefb, texto #16223f, borda #c7d3ee), **aba ativa em azul sólido** (`--blue` + texto #fff) e hover branco; tema claro preservado via override `html[data-theme="light"]` (bg #f8f9fc). **(2) IA no "Novo aviso":** novo bloco "Resumir com IA" com **📎 Anexar arquivo** (`adm-av-file`: .pdf/.txt/imagem) e **✨ Gerar texto com IA** (`adm-av-aigen`). Reaproveita `aiFromFile` (arquivo); para **link**, o front manda `{url}` ao **`/api/ai`**, que passou a buscar o conteúdo **server-side** e injetá-lo no topo do prompt. A IA devolve JSON `{titulo,fonte,tipo,subespecialidade,texto}` que preenche os campos (texto sempre; título/fonte só se vazios; tipo/sub se casar uma opção). **Sem nova função serverless** (limite 12/12 do plano): o fetch ficou em **`lib/fetch-article.js`** (lib/ não conta), com **guarda anti-SSRF** (só HTTPS público; bloqueia loopback/privado/link-local/CGNAT; **revalida cada redirect**; cap 2 MB / 120k chars; aceita só text/html|plain). `api/ai.js` ganhou o param opcional `url` (400 amigável se não extrair texto). **(3) Removido o botão "👁 Pré-visualizar como aluno"** do painel do Mural (era placeholder — só dava um toast, sem função real): tirados o HTML (2 gerações da seção) e o handler `btn-adm-mural-preview`. **CI local verde** (22 arq.; 6/6 inline; 12/12 funções). **⚠️ É JS no frontend → OBRIGATÓRIO testar o PREVIEW da Vercel em navegador real ANTES do merge** (lição OSCE: ci-validate/vm passam e mesmo assim pode quebrar o browser). 
- **Naltrexona-bupropiona adicionada aos mapas de Obesidade (2026-06-22, pedido do usuário "pode incluir") — resolve o item que estava "Aberto":** criado o mapa **por-droga `mm-obe-naltrexona-bupropiona`** em `mm_shared` (**61→62**), espelhando o padrão dos outros fármacos (orlistate/sibutramina/liraglutida/semaglutida/tirzepatida): `topic:'Naltrexona-Bupropiona'`, `sub:'Obesidade'`, `tema:'Tratamento Farmacológico'`, **`tier:'member'`** (entra na metade premium do gate de mapas do #415 — degustação **não** vê, igual a sibutramina/liraglutida/tirzepatida), 4 ramos = **Mecanismo · Posologia · Eficácia e benefícios · Efeitos adversos e contraindicações** (14 folhas), root "Naltrexona-Bupropiona (Contrave/Mysimba)". **É DADO, não código** (igual aos outros mapas de fármaco): `UPDATE jsonb_set` **anexando** ao array `payload.mm_shared` via Supabase MCP, **idempotente** (guard `NOT EXISTS` pelo id) e com **`updated_at=now()`** — o bump é necessário para que um save de admin concorrente entre no caminho de **mescla baseline** (`GLOBAL_MERGE_KEYS`) e **preserve** o novo mapa em vez de sobrescrever. **Sem deploy** (mapas vivem no `endodirect_global_state`; membros veem ao vivo no próximo load). O mapa geral "Farmacoterapia da Obesidade (ABESO 2026)" já citava a droga em "Outras opções" (mantido). **Conteúdo clínico:** bupropiona (inibe recaptação de DA/NE) + naltrexona (antagonista opioide, desbloqueia o autofreio dos neurônios POMC) → ↑ saciedade / ↓ craving; comp 8/90 mg LP, titular em 4 semanas até 2 comp 2×/dia (alvo 32/360 mg/dia); perda ~4–5% vs placebo (estudos COR), pode auxiliar cessação do tabagismo; EA náusea (principal)/constipação/cefaleia/insônia, ↑ PA e FC, **risco de convulsão**; contraindicada em HAS não controlada, epilepsia, bulimia/anorexia, uso de opioides, IMAO, gestação; atenção a ideação suicida (classe antidepressiva). (Obs.: pouco disponível no BR, mas consta nas diretrizes/ABESO.)
- **LME no Memed — é nativo do Memed; era passo de uso, não bug (2026-06-22, investigação a pedido do usuário):** o usuário mandou prints (Memed) onde a insulina tinha a etiqueta **"LME"** mas a emissão saiu como **"Receituário Simples"**, e o nome aparecia como "DEGABRIELLA". **Diagnóstico:** as telas são **do Memed** (terceiro) — quando o Memed está ativo, `initPresc` mostra **só** o módulo Memed e esconde o nosso gerador de LME. **Investigação (suporte Memed):** o Memed **emite LME nativamente**. Fluxo correto: criar a prescrição → buscar o medicamento pelo **princípio ativo** (deve ter a etiqueta **LME** acima do nome) → **Gerar prescrição** → na tela de envio, em **"Retirar no SUS"** clicar em **"Preencher formulário LME"** → preencher campos 1–17 → **Continuar** → assinar e emitir. O usuário pulou o "Retirar no SUS → Preencher formulário LME" e por isso saiu Receituário Simples. **Conclusão: nenhuma mudança de código** — é orientação de uso. O **"DEGABRIELLA"** (sem espaço) é cosmético da UI do Memed ("últimas prescrições de {nome}") — reportável ao suporte Memed, não editável por nós. Decisão do usuário (AskUserQuestion): **"Investigar LME dentro do Memed"** (feito). Nosso `prescPrintLME` (laudo CEAF próprio) segue como fallback quando o Memed está desligado; expor junto ao Memed fica como opção futura se quiser um plano B. Fontes: suporte-medico.memed.com.br (artigo "Como criar uma receita com formulário de alto custo (LME)").
- **E-mails do ciclo de degustação: aviso 48h antes + win-back (2026-06-22, pedido do usuário):** "avisar por e-mail 48h antes do término da degustação; para quem já terminou, e-mail incentivando a assinatura". Infra reaproveitada da newsletter (Resend + cron diário + idempotência em `global_state.payload`). **RPC `endodirect_trial_email_targets()`** (migration `trial_email_targets_rpc`, SECURITY DEFINER, **execute revogado de public/anon/authenticated → só service_role** porque retorna e-mails/PII): varre `endodirect_app_state` por `payload->deg_trials->__start`, exclui admins e quem tem **assinatura/acesso ATIVO** (`endodirect_acessos`/`endodirect_assinaturas`), e classifica `warn` (dia 5–6 dos 7) ou `winback` (dia ≥7), com `days_left`. **`lib/trial-emails.js`** (`sendTrialEmails`): lê a RPC, filtra opt-outs (`newsletter_unsub`) e o **ledger de idempotência** `payload.trial_emails` ({emailLower:{warn,winback}} — cada pessoa recebe cada e-mail **no máx. 1×**), envia via Resend (batch 100, List-Unsubscribe 1-clique), trava `MAX_PER_RUN=300`, grava o ledger sobre o estado fresco. **Acoplado ao cron `endocrine-radar`** (fail-safe; plano limita nº de crons) — roda 10:30 diário. CTA dos e-mails → `PUBLIC_BASE_URL/#planos`. Envs: `RESEND_API_KEY` (sem ela, pula), `SUPABASE_SERVICE_ROLE_KEY`, `TRIAL_FROM`/`NEWSLETTER_FROM`. **Verificado em dados reais (RPC):** hoje 3 `warn` (2 dias restantes) + 3 `winback`. **Backend** → preview não exercita cron (roda só em produção); enviar e-mail real é **outward-facing** → apresentar prévias + contagem e **aguardar aprovação** antes do merge. **CI verde (21 arq.; 12/12 funções — lib/ não conta).**
- **#415 DEPLOYADO + LME (insulinas linha a linha) + painel de criar flashcards = mapas (2026-06-22, pedido do usuário):** **(0)** o usuário aprovou ("Excelente. Pode deployar") → **squash-merge do #415 na main** (subtópicos por subespecialidade em flashcards/mapas no aluno e professor; Editar→Renomear/Mover/Excluir; Ver nos mapas; degustação com metade dos mapas + expiração de 7 dias + upsell; contraste das abas). Antes do merge, **corrigi um bug que eu mesmo introduzi**: em `renderMMList` o índice do botão **Ver** passou a usar `visibleSharedMM()` (filtrado), mas `viewMMShared` lê `sharedMM` (global) → na degustação abriria o mapa errado (e poderia **vazar** um mapa member). Fix: `j=(sharedMM||[]).indexOf(m)` (índice global). CI verde, mergeado, produção deployando. **(1) LME — "as insulinas saíram tudo junto; cada linha deve ser um tipo de insulina (ex.: insulina análoga de ação rápida – lispro); deixar igual ao modelo padrão":** o LME tinha **um** campo de medicamento + **um** conjunto de 6 quantidades (só a linha 1 era preenchida). Refeito para **uma linha por medicamento, com quantidade própria por mês**: a entrada do CEAF de insulina virou `meds:[4 análogas]` (rápida lispro/asparte; prolongada glargina/degludeca); `renderLme` agora monta uma **tabela de linhas** (`#presc-lme-meds`, cada `.lme-mrow` = `.lme-mn` nome + 6 `.lme-mq`) com **"+ Adicionar medicamento"** (`data-act=lmeaddmed`) e 🗑 por linha (`lmedelmed`), via delegação do `prescClick`; helper `lmeMedRowHTML(name)`. `prescPrintLME` lê as linhas do DOM e gera **1 linha da tabela oficial por medicamento** (mín. 5 linhas, cresce se precisar). Removidos os ids antigos `presc-lme-med`/`presc-lme-qt1..6`. **(2) "Deixe a parte superior de criar novos flashcards igual à dos mapas mentais":** no painel do professor, o form de criar flashcards mostrava Frente/Verso **sempre inline** (2 botões); o de mapas tem **3 botões** (Gerar com IA · Importar arquivo · **+ Criar em branco**) e abre o editor sob demanda. Alinhado: `admFlashHTML` agora tem o 3º botão **"+ Criar em branco"** (`adm-fc-blank`) que revela `#adm-fc-manual` (Frente/Verso + "Adicionar card"), escondido por padrão; texto-guia igual ao dos mapas. **(3) "Botões 'Todas as subespecialidades' apagados no dark":** `.dir-back` usava `color:var(--t2)`+`background:var(--surface)` → trocado para `color:var(--tx)`+`background:var(--s2)` (mesmo padrão do `.cat-pill`; hover já era `--blue2`/`--blue-l`). **CI verde; frontend (#416) → nova branch (reset na main) → preview → aprovação.**
- **Degustação: só METADE dos mapas + expiração REAL de 7 dias + upsell + contraste das abas (2026-06-22, pedido do usuário, 3 mensagens):** três pedidos no mesmo fluxo. **(1) "Deixa apenas metade dos mapas atuais no modo degustação":** mapas mentais NÃO tinham trava (todos os 61 visíveis a todos). Implementado o gate por **tier** (igual aos flashcards): **30 mapas marcados `tier:'member'` / 31 livres** (~metade, **balanceado por subespecialidade** — mantidos livres os overview/fundamentais; member os aprofundamentos/diretrizes novas, incl. os 2 de Tireoide ATA 2025 e a maioria dos novos de Obesidade). Tag via `UPDATE jsonb_set` no `mm_shared`. RPC **`endodirect_member_content`** (migration `member_content_mm_shared_tier_gate`): `mm_shared` agora = **plano → todos; degustação (sem escopo) → só `tier<>'member'`; curso-avulso → []** (espelha o fc_shared). **`endodirect_public_content` NÃO filtra mapas** (continua devolvendo os 61) — necessário porque a **conta-demo "Aluno Premium" (alunopro)** carrega por `public_content` e precisa ver tudo (= Gold). No cliente, novo `visibleSharedMM()` filtra `sharedMM` para `isDegustacao()` (só livres) — usado em `mmSubGrid`/`renderMMList`; cobre a **conta-demo de degustação (aluno@)**, que também usa `public_content` (recebe os 61, cliente esconde os member). Resultado: aluno real degustação **31** (server), demo degustação **31** (cliente), demo premium **61** (isDegustacao=false), Gold **61** (server), admin **61**. **Brecha conhecida (aceita):** público não-logado batendo `public_content` direto vê os 61 (mapas nunca foram boundary forte; e a demo premium exige isso). Verificado: member_content sem escopo=31, public=61, fc degustação=221 (inalterado). **(2) "Confirmar que após os 7 dias o usuário não terá acesso a NENHUMA funcionalidade + sugerir assinatura":** CONFERIDO que **NÃO** acontecia — o limite de 7 dias (`DEG_MURAL_DIAS`) valia **só para o Mural**; flashcards/mapas/questões/calc/etc. eram **indefinidos** e a IA era por cota (3 usos), não por tempo. Implementado: `DEG_DIAS=7` + `degExpired()` (= `isDegustacao()` e `Date.now()-degStart()>=7d`) + `degDiasRestantes()`. `canSeePanel`: degustação **expirada → só `perfil`/`support`** (tudo o mais bloqueado → tela de upsell). Nova tela em `showLockedPanel` ("Degustação de 7 dias encerrada — assine para recuperar flashcards, mapas, banco, IA e mural" + CTA `showAssineScreen`). Barra de degustação (`renderDegustacaoBar`) agora mostra **contagem regressiva** ("acesso gratuito por mais X dias") e, ao expirar, "⏳ Degustação encerrada — assine". O relógio (`degStart` em `deg_trials`) **persiste no remoto** para alunos reais (expira de verdade); para a **demo** (sem id) o `deg_trials` é local e é zerado em `clearLocalUserData` a cada troca de conta → a demo nunca trava (sempre demonstrável). `perfil`/`support` seguem abertos p/ a pessoa assinar/logout. **(3) "Botões 'Insuf adrenal'/'Cushing' apagados na tela escura":** as abas de subtópico (`.cat-pill` inativas) usavam `color:var(--t2)` + `background:var(--surface)` (baixo contraste no dark). Trocado para `color:var(--tx)` + `background:var(--s2)` + `font-weight:600` (ativo: `--blue2` + 700). Vale p/ aluno e professor (mesma classe), nos dois temas (via vars). **CI verde.** Backend (tag+RPC) JÁ no ar (afeta degustação real na produção imediatamente); **frontend (index.html) → branch `claude/brave-mayer-8x0lna` → preview → aprovação** antes do deploy. **Aberto:** incluir naltrexona-bupropiona nos fármacos (oferecido); **PR #415** (subtópicos) ainda aguarda "pode deployar".
- **Mapas mentais — Tireoide (ATA 2025) divididos + Obesidade por medicação/modalidade (2026-06-22, pedido do usuário; PUBLICADO direto no Supabase):** o usuário pediu (1) **separar** o mapa combinado "Nódulo Tireoidiano e Câncer" em **dois** mapas "baseados no novo ATA" e (2) na Obesidade, **um mapa por medicação** (Tratamento Farmacológico) e **um por modalidade** (Tratamento Cirúrgico). **Pesquisa (WebSearch):** confirmado o **2025 ATA Management Guidelines for Adult Patients with Differentiated Thyroid Cancer** (publicado dez/2025) — principais novidades assimiladas: **risco de recorrência em 4 níveis** (baixo / baixo-intermediário / intermediário-alto / alto; novos fatores = tamanho, margens, multifocalidade, ETE; N1a<N1b; multifocal eleva risco mesmo em T1; ETE microscópica = menor risco), **lobectomia** como opção se unilateral <4 cm/baixo risco/sem ETE, **complementação não é mais rotina** ("pode ser considerada"), RAI e supressão de TSH **individualizados por risco**, estrutura de decisão **DATA** e resposta dinâmica (excelente/indeterminada/bioquímica incompleta/estrutural incompleta). Nódulo: TSH→cintilografia se baixo, padrões US ATA/TI-RADS, PAAF por padrão×tamanho, Bethesda I–VI, molecular (ThyroSeq/Afirma) em III/IV, vigilância ativa/ablação térmica. **Conteúdo apresentado ao usuário no chat ANTES de publicar** (combinado "IA gera, você revisa e publica") → aprovado ("Pode"). **Como foi publicado:** mapas vivem em `endodirect_global_state.payload.mm_shared` (Supabase, **dado** — não passa por código/git/PR). UPDATE via `jsonb_set`: removido o combinado (`mm-1780536769125`) e adicionados **9** mapas → **mm_shared 53→61**. IDs: `mm-tir-nodulo-ata`, `mm-tir-cdt-ata2025` (Tireoide, `tema` Nódulo/Câncer, 6 ramos cada); `mm-obe-orlistate/sibutramina/liraglutida/semaglutida/tirzepatida` (Obesidade, `tema` Tratamento Farmacológico) e `mm-obe-bypass/sleeve` (`tema` Tratamento Cirúrgico) — 4 ramos cada. **`tema` gravado EXPLÍCITO** em cada mapa (o `temaOf` respeita `item.tema` → cai na aba certa; no preview com o #415, e no painel do professor). **IMPORTANTE — mapas NÃO têm trava de assinatura** (`seedMindmaps` desativado; `renderMMList`/`mmSubGrid` mostram todo `mm_shared` a todos, **inclusive degustação** — comportamento atual dos 53). Logo, os mapas de fármaco/cirurgia também aparecem na degustação (a "trava por assinatura" só existe nos flashcards via `tier:'member'`). Para tornar mapas exclusivos de assinante seria preciso **código novo** (gate de tier em mapas) — fica para a Fase 2. O professor refina cada mapa no painel com **Ver / Editar / Excluir**. **Conteúdos clínicos (drogas):** orlistate (lipase, ~3%, XENDOS), sibutramina (SNRI, contra-indicada em DCV/SCOUT, receita B2), liraglutida 3,0 (GLP-1 diário, ~8%/SCALE), semaglutida 2,4 (GLP-1 semanal, ~15%/STEP, ↓CV/SELECT), tirzepatida (GIP/GLP-1, ~20–23%/SURMOUNT-1, apneia/SURMOUNT-OSA); todas contra-indicadas em CMT/NEM2. Cirurgias: bypass Y de Roux (~30–35%, dumping/hérnia interna/úlcera marginal) e sleeve (~25–30%, fístula no ângulo de His, DRGE de novo). Fontes: ATA patient pages dez/2025 + Executive Summary (Thyroid®). **Aberto (não feito):** incluir naltrexona-bupropiona nos fármacos (oferecido; não marketada no BR).
- **Subtópicos (temas) por subespecialidade em flashcards e mapas mentais (2026-06-22, pedido do usuário):** o usuário pediu (1) mais mapas mentais para assinantes e (2) **subdivisões** dentro de cada subespecialidade (ex.: Diabetes → DM1/DM2/Pré-diabetes/Gestacional/Secundário/Complicações/Tratamento; Tireoide → Hipo/Hiper/Tireoidites/Nódulo/Câncer/Gestação). Decisões via AskUserQuestion: aplicar em **flashcards + mapas** (não em outras áreas); novos mapas **gerados por IA com revisão** antes de publicar. **Fase 1 (este PR) — estrutura:** reaproveita o padrão das Diretrizes (sub → tema). Novo **motor de subtópicos** (`FC_SUBTOPICS` = taxonomia/ordem das abas por subespecialidade; `FC_TEMA_RULES` = regex de classificação; `classifyTema`/`temaOf`/`temaListFor`). **Classificação em tempo de render** (não grava no banco): `temaOf(item,sub)` = `item.tema` explícito OU inferido do enunciado (flashcard) / título (mapa). UI: ao abrir uma subespecialidade aparecem **abas de subtópico** (Todos + temas com contagem) que filtram a lista — em `renderFC` (flashcards, na `fc-filter-row`) e `renderMMList` (mapas, no `mm-subhead`); estados `fcTema`/`mmTema`; handlers `data-fctema`/`data-mmtema` + reset ao trocar de subespecialidade. **Validação:** rodei o classificador no conteúdo real — mapas 51/53 ok (2 em "Geral": Graves pediátrica, Obesidade pediátrica); flashcards Tireoide 18/20, Adrenal 23/25 (resto em "Geral", catch-all). Sem mudança de dados/servidor; tema é derivado. CI verde; frontend → **preview + aprovação**. **Fase 2 (a fazer):** gerar mapas mentais por subtópico (tier `member`, com `tema` explícito) para o professor revisar/publicar — aumenta a quantidade para assinantes. **Taxonomia revisada pelo usuário (2026-06-22):** labels finais aplicados (Diabetes DM1/DM2/Pré/Gestacional/Secundário-MODY/Compl. agudas/Compl. crônicas; Tireoide Hiper/Graves, Nódulo, Câncer; Adrenal labels curtos; Obesidade = Avaliação/Trat. Farmacológico/Trat. Cirúrgico/Comorbidades/Infantil; Lípides = **Hipercolesterolemia/Hipertrigliceridemia/Alfalipoproteinemias**; Neuro sem Cushing; Endo Masculina sem TRT (cai em Hipogonadismo); Transgeneridade = **Homem Trans/Mulher Trans/Monitorização**). **PONTO EM ABERTO:** Diabetes ficou **sem aba "Tratamento"** → os mapas/flashcards de manejo (Hospital, Idoso, Farmacologia, Tecnologia, Diagnóstico) caem em **"Geral"** (5 mapas). Perguntado ao usuário se quer uma aba "Tratamento" (como em Obesidade). **Resolvido (2026-06-22):** o usuário pediu abas granulares no Diabetes → **Diagnóstico, Tecnologia, Hospital, Idoso** (Farmacologia da Glicemia → **DM2**; insulinoterapia → **DM1**); os 13 mapas de Diabetes passam a cair em aba (sem Geral). **Subtópicos no painel do PROFESSOR também** (`admFlashHTML`/`admMapasHTML`, estados `admFcTema`/`admMMTema`) — antes só o aluno tinha. **Edição por item (pedido do usuário):** clicar **Editar** num flashcard/mapa abre painel com **Renomear** (frente/verso ou título), **Mover para** (subespecialidade › aba, dois selects dependentes) e **Excluir** (`admFcEditPanel`/`admMMEditPanel`; mapa tem ainda "🎨 Editar conteúdo" → editor visual). O "mover" grava `sub`/`tema` EXPLÍCITOS em `fcShared`/`sharedMM`; novo índice **`sharedMeta`** (enunciado/título → {sub,tema}) faz a mudança refletir no baralho já semeado do aluno — `subOf()`/`temaOf()` consultam o índice (invalidado em persistFcShared/persistSharedMM/applyStatePayload). Flashcards do aluno passam a agrupar por `subOf` (não só `canonSub`). CI verde.
- **Flashcards — sincronizar os DOIS painéis sem perder nem duplicar (2026-06-22, pedido do usuário após ver o deploy do #411):** o usuário comparou os painéis e apontou que (1) o painel do **aluno** mostrava **Endocrinologia do Esporte** e **Transgeneridade** e o do **professor** não, e (2) os **números não batiam** (prof Diabetes 59 × aluno 6). **Causas:** (a) a conta de **demonstração `alunopro@` ("Aluno Premium")** carrega pela RPC pública `endodirect_public_content`, que entregava **mm_shared** (mapas) mas **NÃO fc_shared** → o aluno-demo via só os ~60 cards de **amostra embutida**, não os 162 publicados; (b) o app injeta **~240 cards de AMOSTRA embutidos no código** (`FC_SEED` 60 + `FC_MEMBER` 180, dedup por front = 240; 20/subesp. × 12, incl. Esporte/Transgeneridade) no baralho do aluno, **distintos** da coleção publicada (confirmado: nenhum dos fronts de amostra está em `fc_shared`) → tiles e números a mais. **Decisão do usuário (dismiss da pergunta + instrução):** "máximo de flashcards em ambos os painéis, **sem perder nenhum e sem duplicidades**" = **manter tudo e unificar** (opção "publicar na coleção"). **Implementação:** (1) **`endodirect_public_content` + `fc_shared`** (migration `add_fc_shared_to_public_content`, espelha mm_shared) → demo/público passam a receber os flashcards publicados, como já recebiam os mapas; (2) **migração única client-side `migrateSamplesToShared()`** (admin, idempotente por ledger `fc_samples_migrated` + dedup por front) — publica `FC_SEED`+`FC_MEMBER` em `fc_shared` com **sub canônico** (`canonSub`: Pediatria→Endoc. Pediátrica, Esportiva→Endoc. do Esporte), via o caminho normal `persistFcShared`; roda no `refreshAfterRemoteState` quando o professor carrega; (3) **`FC_CATS` = 12** (canônicas + Endoc. do Esporte + Transgeneridade); `admFcSubList` (professor) e `fcSubList` (aluno) passam a usar **`FC_CATS`** (+ extras ao final p/ nunca esconder) → **os dois painéis mostram as MESMAS 12 subespecialidades**; `fc-gsub` (Gerar com IA) idem. **Sem prune** do baralho do aluno (não perde nada); o seeding de `fc_shared` deduplica por front e o #411 **promove** cards de amostra legados a "do professor" → sem duplicar. **Resultado-alvo: 402 em ambos** (162 publicados + 240 amostra). DB compartilhado prod/preview → a migração roda quando o professor abre o app (preview ou prod). **GUARDRAILS (pedido do usuário 2026-06-22):** (1) **não perder o conteúdo PESSOAL** de quem paga — verificado: nada faz prune de `DB.fc`/`DB.mm` pessoais; `clearLocalUserData` só roda na troca de conta e é restaurado do estado remoto; o único prune (`DB.mm` `seed:true`) remove cópias redundantes dos mapas do professor, nunca os do aluno (sem flag `seed`); a migração só escreve `fc_shared` (global), nunca o baralho pessoal. (2) **degustação mantém o combinado; a expansão é só para assinante** — implementado por **tier**: na migração, `FC_MEMBER` (biblioteca, 180) recebe `tier:'member'`; `FC_SEED` (60) e os 162 originais ficam livres. As RPCs `endodirect_member_content` e `endodirect_public_content` passam a filtrar `fc_shared`: **plano → tudo (402); degustação/público/curso → só livre (222 = 162+60)**. Migrations `fc_shared_member_tier_gate` e `public_content_fc_shared_free_only` aplicadas e **testadas** (filtro confirma: untagged→todos; `tier:'member'`→excluído p/ não-plano). Resultado: assinante 402, degustação 222 (inalterado), professor vê 402. CI verde; frontend → **preview + aprovação**. **Contagem real pós-migração (2026-06-22):** fc_shared = **401** (221 livre + 180 membro); mm_shared = **53**. Por subesp. (fc total): Diabetes 79, Obesidade 49, Endoc. Pediátrica 48, Adrenal 32, Osteometabolismo 31, Lípides 30, Endoc. Masculina 27, Neuro 25, Tireoide/Feminina/Esporte/Transgeneridade 20 cada. **Demo "Aluno Premium" (alunopro) = Gold real (2026-06-22):** a conta-demo entra sem sessão real (carrega por `public_content` = tier livre) e o seeding pós-hydrate era barrado (`refreshAfterRemoteState` exigia `currentUser.id`) → ela ficava nos ~60 da amostra. Fix: `migrateSamplesToShared` agora também roda para **demo premium** (aluno sem id com `userAcessos` contendo `plano`) montando a coleção completa **localmente** (não grava no servidor); e o gate de seeding passou a incluir contas demo (`currentUser.id || role==='aluno'`), salvando no servidor só quem tem id. Assim a demo mostra os **401** (igual a um Gold), sem afetar degustação (aluno@ sem `plano` segue 222) nem o controle de acesso real.
- **Flashcards "professor 162 / aluno 61" — NÃO era perda de dado; correções pontuais + selo de verificação (#411, 2026-06-22, pedido do usuário "pode investigar"):** o usuário reportou que o painel do professor mostra **162** flashcards publicados e o do aluno **61**. **Investigação no Supabase (dados reais):** `global_state.fc_shared` = **162** cards, todos com frente distinta, sem branco. A RPC `endodirect_member_content()` entrega `fc_shared` para alunos **com `plano` (Gold) OU sem acesso (degustação)** — a única fresta é aluno **só com curso avulso** (sem `plano`), que receberia 0 (mas **hoje não há** nenhum nessa situação). **Causa real do "61":** os dois números medem **coisas diferentes** — 162 = coleção publicada; o "Todos (N)" do aluno = **baralho pessoal** (mistura ~60 amostras embutidas + os do professor + os próprios). Todo aluno que **logou em 21–22/jun já tem os 162** (aparece como **221** = 60 amostra + 161 do professor; **1 dos 162 tem a mesma frente de uma amostra embutida**, então conta como amostra). O "61" era uma conta **antiga/degustação** que **não logava desde antes da publicação** (16–17/jun) — recebe os 162 **no próximo login**. **2 ajustes reais (escolha do usuário via AskUserQuestion = "Corrigir + mostrar X/162"):** (1) **`clearLocalUserData`** agora limpa os ledgers `fc_shared_seeded`/`fc_member_seeded` (antes só `fc`/`fc_seeded`) → ao trocar de conta no mesmo navegador a conta seguinte não recebe mais menos cards (provável origem do "61" que o usuário viu, em navegador de teste); (2) **`seedFlashcards`** — quando um card publicado tem a mesma frente de uma amostra embutida, marca a amostra como `shared` (passa a contar entre os 162, sem duplicar; só toca em cards `seed`, nunca nos criados pelo aluno); (3) **selo no painel do aluno** "X/Y flashcards do professor no seu baralho" (`renderFcProfBadge`, conta por frente; verde quando completo) p/ o professor **verificar** que os publicados chegaram. **Importante (não mexido):** o modelo de **semear no baralho** foi mantido de propósito — flashcards têm **SRS (SM-2: `ef`/`interval`/`reps`/`due`)** por card, diferente dos mapas mentais (que renderizam `mm_shared` direto, sem progresso). Sem mudança no Supabase. **2º pedido do usuário (mesma branch/PR, prints comparando os painéis):** "deixar as subespecialidades do painel do **aluno** iguais às do **professor**, mantendo Novo card / Gerar com IA / Estudar". O painel do aluno usava **pills legadas** (`FC_CATS`: Pediatria/Esportiva/Transgeneridade…, "Todos (N)"); o do professor usa **grade** da taxonomia canônica `MURAL_SUBSPECIALTY_FILTERS` (10). Reescrito: **(a)** `renderFC` agora mostra a **grade de subespecialidades** (mesmo `dir-sub-grid`/`dir-sub-btn`/`dsb-ico`/`dsb-cnt` do professor) e, ao clicar, faz **drill-in** nos cards daquela subesp. (casados por `canonSub`) com botão "← Todas as subespecialidades"; **(b)** nova `fcSubList()` (espelha `admFcSubList`) — canônicas primeiro + extras ao final p/ **não esconder** cards de amostra legados; **(c)** `canonSub` ganhou `Pediatria→Endocrinologia Pediátrica` e `Esportiva→Endocrinologia do Esporte`; **(d)** `FC_CATS` e o `<select>` "Gerar com IA" alinhados às 10 canônicas; **(e)** `startStudy` filtra por `canonSub`; abre na grade ao entrar (`fcFilter=null`). **Esporte/Transgeneridade** (conteúdo de amostra `FC_SEED`/`FC_MEMBER`, ~5–20 cards) aparecem como **tiles extras** ao final. Contagens do aluno ≠ do professor por design (deck = amostra + professor + próprios). **CONCLUÍDO (2026-06-22):** CI verde; #411 mergeada + deploy de produção READY.
- **LME — refeita no formulário OFICIAL do CEAF (2026-06-22, relato do Eduardo):** Eduardo reportou a LME "desconfigurada". Decisão do usuário: reproduzir o **formulário oficial** (Portaria GM/MS 1.554/2013), não o laudo simplificado. Nova função **`prescPrintLME()`** (não usa mais o `prescPrint` genérico do receituário): monta o laudo em **grade de campos numerados** — 1.CNES · 2.estabelecimento · 3.paciente · 4.nome da mãe · 5.peso · 6.altura · 7.medicamento + 8.quantidade por mês (1º/2º/3º) · 9.CID-10 · 10.diagnóstico · 11.anamnese · 12.tratamento prévio · 14.médico · 15.CNS · 16.data · 17.assinatura/carimbo · 18–23 (paciente). A aba LME (`renderLme`) ganhou os inputs novos (estabelecimento/CNES/CNS-médico **persistidos** em `presc_prescritor`; nome da mãe, peso, altura, CNS do paciente, diagnóstico, quantidades, tratamento prévio). Campos oficiais via WebSearch (gov.br/SES). CI verde; **prévia HTML enviada** ao usuário (gerada rodando a própria função com dados de exemplo). Não dá p/ rasterizar HTML no sandbox (sem Chromium) → validação visual é no preview/pelo Eduardo. Frontend → preview + aprovação. Ver [[Integrações]].
- **Calculadora PREVENT — investigação "diferente da AHA" + modelo aprimorado UACR/HbA1c (2026-06-22, pedido do usuário):** o usuário comparou a calculadora de risco CV (PREVENT) com a oficial da AHA e viu números diferentes. **Investigação: NÃO era bug** — rodei o código real com os dados dos prints: base feminino dava 17,37/24,41/12,05 (idêntico à tela). A divergência era comparação desigual: **(1) sexo diferente** (app=Feminino, print da AHA=Male → DCV total 24,4%→30,6%) e **(2)** o site da AHA usou as **"full equations"** com **UACR 45 + HbA1c 6,9** (modelo aprimorado), enquanto o app tinha só o **modelo base**. **Implementação (a pedido):** campos opcionais **RAC/UACR (mg/g)** e **HbA1c (%)** nas calculadoras PREVENT 10a e 30a. Preenchidos, usam as equações **aprimoradas** oficiais (variantes uacr/hba1c/full; SDI tratado como ausente, sem zip BR). **Coeficientes** oficiais (Khan, Circulation 2024) extraídos do pacote R **`preventr`** (`R/sysdata.rda`, lido com `pyreadr`) — não transcritos à mão. **Validação:** harness Node vs. referência Python (24 casos, dif ≤1e-6) + **âncora externa**: masculino +UACR45/HbA1c6,9 = **31,3% DCV total**, igual ao site oficial da AHA. Modelo **base inalterado** com campos vazios (saída idêntica). CI verde. Frontend → **preview + aprovação** antes do deploy. **LIÇÃO:** "PREVENT ≠ AHA" quase sempre é comparação desigual (sexo / UACR / HbA1c / SDI) — conferir antes de supor bug. Ver [[Calculadoras]].
- **Instagram "Questão do Dia" em Stories — direção definida + amostra (2026-06-22, pedido do usuário; NÃO construído):** o usuário quer um post diário de questão no Instagram **@endodirect** (conta **já Business**), **somente em Stories** (9:16, 1080×1920), às **18h BRT**. Decidido: **rota C (híbrido com aprovação)** — o sistema gera arte+legenda+gabarito, o professor aprova e **posta manualmente** no MVP (migra p/ Graph API depois). Conteúdo **misto**: banco TEEM como **calibragem**, mas vai ao ar **caso ORIGINAL** (IA+revisão) — **não repostar TEEM verbatim** (direito autoral SBEM). Nuance Stories: quiz nativo ≤4 opções / poll =2, e **stickers não vão por API** → reforça aprovação + post manual. **Amostra** (2 slides pergunta/gabarito; caso de hiperaldosteronismo, resposta B) gerada **localmente** via `@resvg/resvg-js` (SVG→PNG, on-brand navy+dourado); **Canva AI falhou** com texto clínico longo+brand kit (lição: usar brand template com autofill ou render próprio). MVP proposto: `lib/instagram.js` (módulo) + card de aprovação no painel + aviso diário por e-mail pegando carona no cron do radar (teto 12 funções). Aguardando go-ahead. Ver [[Instagram Stories (Questão do Dia)]].
- **Obesidade — ADA Standards of Care (tratamento farmacológico) assimilado de PDF do Google Drive (#404, 2026-06-21):** o usuário apontou a pasta **"Plataforma ED"** no Drive (via MCP Google Drive) com o PDF `standards of care - obesidade - tratamento.pdf` (deck do ADA Scientific Sessions 2026, L. Aronne — Standards of Care em Sobrepeso e Obesidade). Li com `read_file_content`. +2 bullets no `CLINICAL_GUIDELINES`, só com o que era NOVO vs. o já assimilado (ABESO/SBEM 2024 + trials de incretina): **(1)** framework ADA — farmacoterapia PRECOCE (parte do tratamento inicial, Grau A), tiers de eficácia (alta >10% tirzep/sema; moderada fentermina-topiramato; modesta naltrexona-bupropiona/liraglutida/orlistate/fentermina), continuar a longo prazo, dose de manutenção individualizada, preservar músculo (proteína+força), não usar manipulados, trocar em desabastecimento, cadência de seguimento; **(2)** escolha da droga pela COMORBIDADE com graus (prevenção DM2 [RRR 37–93%], PA, glicemia, MACE [SELECT −20%], ICFEp [STEP-HFpEF/SUMMIT], MASH, AOS [SURMOUNT-OSA], dor na OA). `node --check` OK. Só IA. **NOVO no fluxo:** dá p/ puxar arquivos do Google Drive via MCP (`search_files` por `parentId`, `read_file_content` lê PDF) — a pasta "Plataforma ED" vira canal de entrega de material. Frontend → PR + preview + aprovação.
- **3º lote de literatura — Pituitary / Archives of Endocrinology and Metabolism (SBEM) / Endocrine Reviews — +9 na IA (#403, 2026-06-20):** o usuário pediu essas 3 revistas (3 anos). Levantamento no PubMed; +9 bullets no `CLINICAL_GUIDELINES`, priorizando o brasileiro (AE&M/SBEM) e profundidade hipófise/Cushing: **(1)** obesidade-farmacoterapia (ABESO/SBEM 2024); **(2)** macroprolactinemia (SBEM/SBPC-ML 2025) — PEG, evita investigação desnecessária; **(3)** iodo na gestação (SBEM Tireoide 2025); **(4)** hipogonadismo feminino (síntese 2025; POI/Turner); **(5)** quilomicronemia familiar/FCS (consenso 2025) — anti-apoC-III; **(6)** acromegalia-complicações (Consenso 2024); **(7)** tumores hipofisários agressivos/carcinomas (ESE 2025) — temozolomida 1ª linha; **(8)** diagnóstico de deficiência de GH (GH Research Society 2025); **(9)** tratamento medicamentoso de Cushing (Endocr Rev 2026). Recomendações conferidas nos abstracts. `node --check` OK. Só IA. Frontend → PR + preview + aprovação.
- **PEP (prontuário eletrônico do paciente) no Consultório — avaliação de viabilidade (2026-06-20, pergunta do usuário; NÃO implementado):** VIÁVEL tecnicamente. Modelo atual = Supabase com `endodirect_app_state` (blob JSON por usuário) + admins/acessos/assinaturas/devices; **não há tabelas de paciente**. Para PEP: criar tabelas novas (`pacientes`, `atendimentos/evolucoes` SOAP, `prescricoes`, `anexos` em Storage) com **RLS por médico (user_id)**, reaproveitando o Consultório (`#panel-presc`) e o receituário/Memed (autopreencher paciente). MVP faseado: (1) cadastro de pacientes (CRUD); (2) evolução/anamnese + linha do tempo; (3) vincular receitas/exames/anexos. **Gargalos NÃO técnicos (decisivos):** LGPD (dado sensível — base legal/consentimento, criptografia, logs, retenção; EndoDirect = operador → contrato); CFM (prontuário que SUBSTITUI papel exige certificação SBIS/CFM NGS1/NGS2 + assinatura ICP-Brasil; Resol. CFM 1.821/2007; guarda 20 anos). Recomendação: começar como registro clínico interno (não-certificado) reusando a assinatura ICP-Brasil da Memed p/ receitas e evoluir p/ certificação se quiser eliminar papel. Decisão do usuário pendente.
- **Receituário de Controle Especial: genérico não era reconhecido como controlado (#402, 2026-06-20):** o usuário (Eduardo, no Android) mandou 2 prints — com o **nome genérico "Sertralina"** o app marcava **"Receita Simples"** e recusava emitir como Controle Especial (*"Nenhum medicamento de controle especial na lista"*); com a **marca "Tolrest"** (= sertralina) emitia certo como especial. **Causa:** no `PRESC_MEDS` (curado), **sertralina e fluoxetina** estavam `r:'simples'` (erradas — ambas são **C1**, controle especial em 2 vias). O merge do catálogo ANVISA (`prescLoadCatalogo`) **pula** entradas cujo nome já existe no curado (`if(seen[key])return`), então o regime correto da ANVISA ("Cloridrato de Sertralina"/"Cloridrato de Fluoxetina" = **especial** no catálogo) nunca se aplicava ao genérico curado. **Fix:** sertralina e fluoxetina → `r:'especial'`. Varri o resto do curado: demais controlados OK (sibutramina `b2`, topiramato/testosterona `especial`, clonazepam/zolpidem/diazepam `b1`). Validado por harness Node (extrai `prescNorm`/`prescClassify`/`prescIsEspecial`/`PRESC_MEDS`): Sertralina/Fluoxetina→especial; metformina/losartana→simples. `node --check` OK. **LIÇÃO:** com lista curada + catálogo externo e a regra "curado vence em nome duplicado", um regime errado no curado mascara o certo do catálogo — manter os regimes do curado corretos. Frontend → PR + preview + aprovação.
- **2º lote de literatura (mesmas 5 revistas) — +7 primers/consensos na IA (#401, 2026-06-20):** continuação do #399 (abstracts já levantados no PubMed). +7 bullets no `CLINICAL_GUIDELINES`: **(1)** MASLD/MASH (Nat Rev Dis Primers 2025) — nomenclatura 2023, dx = esteatose + ≥1 critério cardiometabólico, FIB-4/elastografia, resmetirom (FDA 2024); **(2)** pré-diabetes (2025) — GJA vs intolerância (fisiopatologias distintas); **(3)** síndrome metabólica (2024) — critérios + estilo de vida; **(4)** feo/PGL pediátrico (Nat Rev Endocrinol 2024) — 70–80% hereditário, mais metastático; **(5)** acondroplasia/vosoritida (2025) — 1º tratamento de precisão; **(6)** adenomas hipofisários pediátricos (2024) — mais ocultos/agressivos/genéticos; **(7)** hiperferritinemia metabólica (2023) — sem benefício de flebotomia de rotina. **Excluídas (fora de escopo):** diretrizes ES de **menopausa, hipertrigliceridemia e osteoporose no homem** — na varredura 2023–26 só apareceram como *Correction*; as originais são de 2012–2015 (fora dos 3 anos) → não assimiladas (avisado ao usuário). `node --check` OK. Frontend (IA) → PR + preview + aprovação.
- **Receituário de Controle Especial: faltavam endereço do emitente e campos obrigatórios (#400, 2026-06-20):** relato do Eduardo (no Android) com print do preview de impressão — o **Receituário de Controle Especial** saía só com Paciente (nome+idade), medicamento, assinatura (nome+CRM/UF) e data; **não aparecia o endereço do emitente** nem outros campos exigidos pela **Portaria 344/98**. Causa: `renderPrescTool`/`prescPrint` (`index.html`) não tinham **campo nem renderização** de endereço (do emitente ou do paciente). Fix: **(1) formulário** — inputs novos `presc-presc-end` (endereço do emitente/consultório), `presc-presc-fone` (telefone) e `presc-pac-end` (endereço do paciente), persistidos em `presc_prescritor` (endereco/fone) via `Object.assign` (não apaga cpf/nascimento usados na aba Memed). **(2) Documento** — bloco "Identificação do emitente" (nome + CRM/UF + esp + endereço + telefone) no topo de toda via; endereço do paciente no bloco do paciente; quadros **"Identificação do comprador"** e **"Identificação do fornecedor (farmácia)"** só no Controle Especial (`vias=true`). **(3) CSS** dos blocos + `.bd` min-height 220→150px (reduz o vão enorme que aparecia no print). Validado extraindo a `via()` real e gerando o HTML (todos os campos presentes; comprador/fornecedor só no especial); `node --check` OK. **LIÇÃO:** documento legal (receita) exige os campos regulatórios completos — emitente COM endereço é obrigatório; conferir contra a Portaria 344/98. Frontend → PR + preview + aprovação.
- **Assimilação JCEM / Nat Rev Endocrinol / Nat Rev Dis Primers / JBMR(ASBMR) / Obesity — 10 diretrizes-consensos na IA (#399, 2026-06-20):** o usuário pediu p/ assimilar os últimos 3 anos dessas 5 revistas (endócrino). Levantamento via **PubMed** (busca por revista + guideline/consensus/statement). Curados os de ALTO RENDIMENTO que preenchiam LACUNAS no `CLINICAL_GUIDELINES` (+10 bullets, @~L3216): **(1)** Vitamina D (Endocrine Society 2024) — não rastrear 25OHD na população saudável; suplementar empírico só em crianças/gestantes/≥75a/pré-diabetes; **(2)** Prolactinoma (Pituitary Society 2023) — cabergolina 1ª linha mesmo em macro, efeito gancho, retirada do AD; **(3)** Acromegalia (Consenso 2025) — meta IGF-1/GH, cirurgia 1ª linha, SRL/pegvisomanto/cabergolina; **(4)** Cushing — rastreio (salivar noturno/UFC/dexa 1mg)+ACTH, cirurgia 1ª linha; **(5)** Feo/paraganglioma (2023–24) — metanefrinas, ~40% hereditário, SDHB agressivo, alfabloqueio pré-op; **(6)** SOP (Diretriz Internacional 2023) — Rotterdam c/ AMH alternativa em adultas (não em adolescentes), letrozol 1ª linha; **(7)** Osteoporose treat-to-target (ASBMR/BHOF 2024); **(8)** XLH (IWG 2025) — burosumabe > convencional; **(9)** DM pré-existente e gestação (ES/ESE 2025); **(10)** Puberdade precoce central (ES 2026). Só IA (escopo "curado de alto rendimento", confirmado); sem mapas/flashcards. Recomendações conferidas nos abstracts do PubMed. **LIÇÃO (PubMed MCP):** `get_article_metadata` corta em **20 artigos/chamada** e estoura tokens (salva em arquivo) → buscar por fonte, baixar em lotes ≤20 e usar **`jq`** nos arquivos salvos; o `jq` aborta a saída inteira se um campo der erro de índice (usar `?`/`//` p/ proteger, ex.: `.identifiers.pmid?`). **Pendência (2º lote, se quiser):** MASLD nomenclatura, pré-diabetes/síndrome metabólica (primers), feo/PGL pediátrico, acondroplasia/vosoritida, hiperferritinemia metabólica, menopausa/hipertrigliceridemia/osteoporose no homem (ES). Frontend (IA) → PR + preview + aprovação.
- **Calculadora PREVENT (AHA) + evidência NEJM/Lancet 2023–2025 na IA (#397, 2026-06-20):** o usuário pediu (a) implementar o escore **PREVENT** de risco CV da AHA na seção de Calculadoras e (b) assimilar os últimos NEJM/Lancet de endocrinologia (3 anos) p/ a IA; depois mandou o link da calculadora oficial (`professional.heart.org/.../prevent-calculator`). **(a) PREVENT (`index.html`):** nova calculadora `id:'prevent'` (área "Risco cardiovascular") — modelo **BASE de 10 anos**, headline = **DASCV** com categoria (baixo <5 / moderado 5–20 / alto >20%, Diretriz Brasileira 2025) + **DCV total** e **IC** na tag. Helper `preventRisk(v)` + `PREVENT_COEFS` (antes do `var CALCS`). Campos: sexo, idade(30–79), CT, HDL, PAS, TFGe, IMC, diabetes, tabagismo, anti-HAS, estatina. **Coeficientes do pacote R validado `bcjaeger/PooledCohort`** (a API do GitHub e o rdrr.io davam **403**; o `preventr` guarda em `sysdata.rda` **binário**) — **clonei** o repo (`git clone --depth 1`), li `data-raw/coefs_prevent.xlsx` (abas `base_10`/`base_30`; colunas women/men × cvd/ascvd/hf/chd/stroke) via **parser zlib/XML em Python** (não há pandas/openpyxl no container) e **gerei o literal JS da MESMA fonte** (zero transcrição manual). **VALIDADO** contra o caso-ouro do teste do pacote (suplemento **Khan, Circulation 2024, Tabela S12**): F **9,20%** / M **10,19%** de DASCV (idênticos) → confirma transformações (não-HDL e HDL em mmol/L com fator 0,02586; splines de PAS<110/≥110 e TFGe<60/≥60; interações com idade) + link logístico `exp(s)/(1+exp(s))` + os coeficientes. Sintaxe do `index.html` checada com `node --check` em todos os `<script>`. **(b) `CLINICAL_GUIDELINES` (+3 bullets, @~L3190):** incretinas no peso (SURMOUNT-5 tirz>sema −20,2 vs −13,7%; CagriSema −20,4%; retatrutida ~24% fase 2; orforglipron oral −11% ATTAIN/ACHIEVE, sem jejum), desfechos (SOUL sema **oral** HR 0,86 — 1º GLP-1 oral c/ benefício CV; SURPASS-CVOT tirz não inferior à dula HR 0,92; SUMMIT tirz na ICFEp+obesidade; STRIDE sema na DAP; ESSENCE sema 2,4 na MASH resolução 62,9 vs 34,3% e fibrose 36,8 vs 22,4%), DM1 (teplizumab/PROTECT 1ª imunoterapia modificadora; icodec basal **semanal**/ONWARDS — preferir DM2). Números conferidos via **PubMed** (abstracts). **LIÇÃO (extrair coef. de pacote R):** com API/rdrr.io bloqueados e dados em `.rda`/`.xlsx` binário, **clonar o repo e parsear o `data-raw`** (xlsx = zip de XML → zlib/regex puro) > caçar números em fontes renderizadas; **sempre validar contra um caso de referência do próprio pacote** antes de commitar (calculadora clínica = erro vira risco errado). Frontend → PR + preview + aprovação. **30 anos (#398, 2026-06-20):** o usuário pediu a variante de 30 anos → adicionei `id:'prevent30'` (aba `base_30`, mesma fonte/equação; `preventRisk` agora retorna também `ascvd30`/`cvd30`/`hf30`), calculada só para 30–59 anos, **validada** (Khan S12: F 35,4% / M 34,9%). Mergeado + deploy READY.
- **Hiperparatireoidismo primário (5º Workshop Internacional 2022, JBMR) assimilado (#396, 2026-06-19):** o usuário enviou o PDF da diretriz (*summary statement/guidelines* do Fifth International Workshop, GRADE). **LIÇÃO de extração de PDF:** neste container NÃO havia `pdftotext` e o `cryptography` estava quebrado (`_cffi_backend` ausente → pypdf/pdfminer/PyPDF2 todos falharam na instalação/import). **Solução que funcionou: zlib puro da stdlib** — `re.finditer(rb'stream...endstream')` + `zlib.decompress` em cada stream FlateDecode + extrair as strings `(...)` e filtrar a marca d'água do Wiley. Guardar essa técnica p/ quando faltar poppler. **(a) IA (`CLINICAL_GUIDELINES`):** +1 entrada (Hiperpara primário 2022) — dx (hipercalcêmico: Ca↑ + PTH alto/inapropriadamente normal em 2 ocasiões ≥2sem; normocalcêmico: Ca total+iônico normais + PTH↑ por 3–6m após excluir 2ª; DDx HHF [Ca/Cr urinária <0,01]/tiazídico/lítio), avaliação (DXA 3 sítios incl. rádio distal ⅓ + VFA + calciúria 24h), **critérios de cirurgia** (Ca >1,0 mg/dL acima do LSN; T-score ≤−2,5 ou fratura vertebral; TFGe/clearance <60 / nefrolitíase-nefrocalcinose / calciúria >400 mg/24h; idade <50, usar Z-score), tratamento clínico (cinacalcete p/ baixar Ca; alendronato/denosumabe p/ DMO; 25OHD >30; raloxifeno não), seguimento (Ca+25OHD anuais, DMO 1–2a) e gestação (cirurgia 2º tri se Ca >11, só USG, sem denosumabe). Subespecialidade canônica = **Osteometabolismo** (o `canonSub`/categorizadores já roteiam hiperpara/PTH p/ lá). **(b) Aos alunos (Osteometabolismo):** publicar mapa + flashcards em `mm_shared`/`fc_shared`. Escopo "IA + mapas/flashcards" aprovado via AskUserQuestion. **Lote (mesmo dia):** o usuário enviou +2 PDFs — **(i)** `bilezikianetalguidelines.pdf` = **MESMO** paper (JBMR 4677, confirmado pelas refs internas `JBMR_4677`) → já assimilado, ignorado; **(ii)** **NOGG 2024** (National Osteoporosis Guideline Group, UK — osteoporose) → +1 entrada no `CLINICAL_GUIDELINES` no MESMO PR #396: FRAX + limiares NOGG (baixo/alto/muito alto), risco iminente pós-fratura, tratar alto/muito alto; **muito alto = anabólico 1º** (teriparatida/abaloparatida/romosozumabe) → antirreabsortivo sem demora; alto = bisfosfonato oral ou zoledronato IV 5mg/ano (denosumabe opção); **terapia sequencial obrigatória** após parar denosumabe/anabólico (rebote); sem "drug holiday" se segue de risco; cálcio dieta + vit D ≥800 UI/d. **Lição extra:** o regex de stream do extrator zlib precisa ser `stream\\r?\\n(.*?)endstream` (o NOGG não tinha `\\n` antes de `endstream` → 0 chars com o regex antigo). Subesp. de ambos = **Osteometabolismo**. Mudança da IA (`index.html`) → PR + preview + aprovação. **CONCLUÍDO (2026-06-20):** #396 mergeado + deploy de produção READY; conteúdo de aluno publicado em Osteometabolismo — `mm_shared` 51→**53** (`mm-ost-hpt-2022`, `mm-ost-nogg-2024`) e `fc_shared` 151→**162** (`fc-ost-hpt-1..6`, `fc-ost-nogg-1..5`).
- **Memed: abertura mais rápida com pré-aquecimento (#395, 2026-06-19):** o médico pediu pra reduzir a espera entre clicar em "Abrir prescrição Memed" e o módulo aparecer. As 2 latências do clique eram (1) o POST `/api/memed/token` (backend→Memed) e (2) o download do SDK de produção (`partners.memed.com.br/integration.js`). Fix em `index.html`: ao renderizar o Consultório, `prescPrewarm()` (300ms após o render) faz **preconnect + preload** do SDK e **busca o token antecipadamente** em background (só se o prescritor já está completo/pré-preenchido), cacheando em `_prescWarm` com uma **assinatura** dos campos do prescritor (`prescSig`) + timestamp. No clique, `prescMemedOpen` usa o token pré-aquecido se a assinatura bate e tem < 3 min (pula `getSession`+fetch); senão, fluxo normal. **Segurança:** qualquer erro na abertura zera `_prescWarm` (o retry busca token fresco); o preload usa a MESMA URL do `<script>` (sem `crossorigin`, pra reusar o cache). `setPaciente`/`module.show` continuam no `core:moduleInit` (não pré-abre o módulo). Reaberturas já eram rápidas (SDK em memória); o ganho é no 1º clique. Frontend → PR + preview + aprovação.
- **Memed: histórico de emissões SEM PII do paciente + política de privacidade (#394, 2026-06-19):** o usuário perguntou se a proteção de dados do paciente é toda da Memed. Auditei o fluxo: **CPF/e‑mail/telefone do paciente vão só p/ a Memed** (client-side via `setPaciente`), o **prescritor** passa pelo `api/memed/token.js` só p/ cadastro na Memed (não gravado no nosso banco) — **MAS** o `presc_emitidas` (histórico de emissões, em `PERSONAL_STATE_KEYS`/`REMOTE_STATE_KEYS`, sincronizado no `endodirect_app_state` por usuário com RLS) guardava o **nome do paciente** + o payload cru do evento `prescricaoImpressa`. A pedido do usuário ("zero PII de paciente do nosso lado"): (a) `presc_emitidas` passou a guardar **só `{ts, rxId}`** — data/hora + ID opaco da receita (extraído defensivamente do evento via `prescRxId`), **sem nome e sem o `data` cru**; (b) `prescScrubLog()` roda no boot (no `.then` do `hydrateRemoteState`) e **limpa entradas legadas** `{paciente,prescritor,data}` → `{ts,rxId}` (do teste do médico), sincronizando a versão limpa; (c) **`privacidade.html`** atualizada — bullet do Consultório/Memed (paciente vai direto à Memed, **não armazenamos**; só registro de emissão sem identificação) + Memed na lista de operadores + data 19/06. **Pendência formal (lado do usuário):** DPA (acordo de tratamento de dados) com a Memed. Ver [[Integrações]].
- **Consultório (Memed): limpeza estética + paciente obrigatório (#393, 2026-06-19):** com o Memed já renderizando em produção, o usuário pediu ajustes na tela do Consultório. (1) **Removidos** o subtítulo "Apoio à prescrição com receituário, LME, exames e seguimento…" (`<p>` do header `#panel-presc`) e o **banner** "⚠️ Ferramenta educacional de apoio à decisão…". (2) **Campos de data/e-mail "bonitinhos":** a regra global de input (`@~L225`) cobria só `text/search/number/select/textarea`, deixando **`type=date` e `type=email` SEM tema** (sem `width:100%`, fundo claro, ícone do calendário invisível no escuro) — por isso a "data de nascimento" e o "e-mail do paciente" pareciam quebrados. Adicionados `email/date/tel/url` ao seletor + **`color-scheme:dark`** (conserta também autofill amarelo e o date-picker nativo). Beneficia de quebra a data das calculadoras (`@~L5387`) e o e-mail do perfil (`@~L8634`); **login** (`.login-body .fld input`) e **checkout** (`#checkout-modal .ck-fld input`) ficam intactos (seletor mais específico vence). (3) **CPF do prescritor e do paciente agora OBRIGATÓRIOS:** removido o "(opcional)" dos rótulos (o CPF do prescritor já era validado em 11 dígitos — só faltava o rótulo) e o **paciente passou a exigir nome + CPF (11 díg.)** no `prescMemedOpen` antes de abrir o módulo (o `setPaciente` da Memed exige o nome, então CPF sem nome não renderiza o paciente). Convenção de "obrigatório" no app = rótulo SEM "(opcional)". Doc Memed (prints do usuário) confirma a abordagem do front: carregamento **dinâmico** (`createElement`+`script.dataset.token`=`data-token`) + rodar a lógica só no evento `load` — exatamente o que o `prescMemedLaunch` faz. Frontend → PR + preview + aprovação.
- **Memed "Módulo aberto" mas a tela trava = SDK do front-end era de HOMOLOGAÇÃO com token de PRODUÇÃO (#392, 2026-06-19):** depois do hotfix #391 (POST `/api/memed/token` → 200, token do prescritor de produção recuperado), o usuário reportou que voltou a aparecer "Módulo Memed aberto" mas **a tela trava e não renderiza nada**. **Diagnóstico (empírico, sem chutes):** `GET /api/memed/token` em produção (via `mcp web_fetch_vercel_url`) retornava `configured:true`, `allow:["rodolphomend@gmail.com"]` e **`script:"https://integrations.memed.com.br/...sinapse-prescricao.min.js"`** — ou seja, o **SDK de HOMOLOGAÇÃO**. Os logs da Vercel confirmaram POST→200 (e os 400 antigos eram "já cadastrado", comportamento **só de produção**) → o `MEMED_API_BASE` é produção e o **token é de produção**. **Token de produção + SDK de homologação = o módulo "abre" (a mensagem aparecia cedo demais, ao registrar o listener) mas o iframe não autentica → tela travada/branca** (bate com a doc Memed "O módulo da Memed não carrega" e com a regra do SDK: `integrations.memed.com.br` é **só homologação**). **A URL de produção é OUTRA e bem diferente** (o usuário confirmou no painel Memed → Front-end → Configurações → URLs): **homologação = `https://integrations.memed.com.br/modulos/plataforma.sinapse-prescricao/build/sinapse-prescricao.min.js`**; **produção = `https://partners.memed.com.br/integration.js`** (`MEMED_SCRIPT_URL`). **Fix (2 partes):** (a) **`api/memed/token.js`** — o default do `MEMED_SCRIPT` deixou de ser fixo no de homologação e passou a ser **DERIVADO do ambiente do `MEMED_API_BASE`** (`integrations.api.memed` → SDK de homologação; senão → SDK de produção `partners.memed.com.br/integration.js`), pra **nunca dessincronizar**; `MEMED_SCRIPT` (env) ainda sobrescreve. (b) **`index.html` `prescMemedLaunch`** — (1) passou a **aguardar o `MdSinapsePrescricao` aparecer** (poll ~8s) porque o loader de produção (`integration.js`) injeta o SDK de forma assíncrona (≠ do `.min.js` que já o define no onload); (2) a Promise **só resolve quando o módulo realmente renderiza** (`MdHub.module.show` no `core:moduleInit`), então **"Módulo Memed aberto" deixou de ser mentiroso**; (3) **timeout de ~20s** com erro claro ("SDK e token de ambientes diferentes") em vez da tela travada; (4) reabertura mostra o módulo direto (o `core:moduleInit` não redispara). **LIÇÃO:** valeu NÃO chutar a URL de produção (cofre: "fix especulativo custa um ciclo de deploy") — meus palpites (`elements`/`prescricao`/`app.memed.com.br`) estavam todos errados; a real é `partners.memed.com.br/integration.js`. Para diagnosticar SDK gated por ambiente, **ler o que o backend serve de fato** (`GET /api/memed/token` é público) > teorizar. **Deployado e CONFIRMADO ✅:** #392 mergeado, deploy de produção `READY`; `www.endodirect.com.br/api/memed/token` passou a servir `partners.memed.com.br/integration.js` e o **médico (rodolphomend@) confirmou que o módulo RENDERIZA** em produção — bug resolvido. Ver [[Integrações]], [[Pendências]].
- **Diagnóstico de diabetes: incluído o TOTG de 1h (SBD/IDF) no mapa e flashcard, marcado como fora da ADA (2026-06-18):** o usuário (médico) notou que a ADA 2026 **não** contempla a glicemia de 1h no TOTG, mas a **SBD/IDF** sim. Atualizados **só os itens de aluno** (no banco, sem deploy): flashcard `fc-dia-dx-1` e mapa `mm-dia-diagnostico-ada2026` passaram a incluir **1h ≥209 mg/dL = diabetes** e **1h 155–208 mg/dL = pré-diabetes (hiperglicemia intermediária IDF)**, ambos rotulados **(SBD/IDF)*** com nota de rodapé "*TOTG de 1h: critério SBD/IDF, NÃO contemplado pela ADA 2026" (o mapa ganhou uma ramificação "Observação"). Inicialmente deixei o `CLINICAL_GUIDELINES` (IA) intacto, mas **em seguida o usuário pediu para incluir lá também (#387)** — então a entrada de diagnóstico do `CLINICAL_GUIDELINES` agora cita o 1h ≥209 (diabetes) e 155–208 (pré-diabetes) como **(SBD/IDF, não contemplado pela ADA 2026)**. Assim a IA passa a considerar o 1h sinalizando que é SBD/IDF. Cortes IDF: 1h ≥155 (intermediário) e ≥209 (diabetes).
- **ADA Standards of Care 2026 — 2ª leva: +7 capítulos (#384, 2026-06-18):** continuação da assimilação da ADA 2026 (em lote, deploy único). Capítulos **9 (Farmacologia da Glicemia), 10 (Cardiovascular), 11 (DRC), 12 (Retinopatia/Neuropatia/Pés), 13 (Idosos), 14 (Crianças/Adolescentes), 16 (Hospital)** — todos lidos via **`pdftotext`** (a lição do render). **(a) `CLINICAL_GUIDELINES` 23→30** (7 entradas; PR #384 acumulou os 7 antes do merge). **(b) Mapas/flashcards** em **"Diabetes"** — `mm_shared` 45→**52** (7 mapas `mm-dia-{farmaco,cardiovascular,renal,microvascular,idoso,pediatria,hospital}-ada2026`) e `fc_shared` 116→**151** (35 flashcards `fc-dia-{farm,cv,ren,mic,ido,ped,hosp}-1..5`). **Conteúdo-chave:** DM2 guiado por risco cardiorrenal (IC→iSGLT2; DRC→iSGLT2±GLP-1/finerenona; DASCV→GLP-1/iSGLT2, independe da glicemia); metformina 1ª linha. CV: hipertensão ≥130/80, alvo <130/80; LDL <55 (DASCV) / <70 (alto risco); AAS 75–162 na 2ª prevenção. DRC: UACR+eGFR anual; iSGLT2 eGFR ≥20; finerenona; IECA/BRA. Microvascular: retina (DM1 5a / DM2 ao dx); neuropatia (monofilamento 10g + 128Hz; dor → gabapentinoide/SNRI/amitriptilina, sem opioide); pés anual. Idosos: A1C por saúde (<7–7,5 / <8 / evitar A1C+hipo, PA <140/90). Pediatria: DM1 basal-bolus <7%; DM2 jovem agressivo (metformina ± insulina, GLP-1/iSGLT2). Hospital: A1C na admissão, meta 140–180, basal-bolus-correção (não sliding scale). **Processo:** lote acumulado em UM PR (#384) ao longo de vários envios; o usuário disse "aguarda pra fazer deploy de uma vez. Tem mais artigos" e, ao final, "pode publicar" → mergeei + publiquei mapas/flashcards de uma vez. **Total do dia: `CLINICAL_GUIDELINES` 12→30 (18 diretrizes novas).** Faltaram (se o usuário quiser): caps. 1, 4, 5, 15 da ADA.
- **ADA Standards of Care 2026 — 5 capítulos assimilados em lote (#382, 2026-06-18):** o usuário enviou em lote os caps. **7 (Tecnologia), 8 (Obesidade/Manejo de Peso), 2 (Diagnóstico e Classificação), 3 (Prevenção) e 6 (Metas Glicêmicas, Hipoglicemia e Crises)** do *Diabetes Care 49(Supl.1), jan/2026* ("assimile tudo / fique cada vez mais inteligente e atualizado"; "acrescentando mais artigos antes de deployar" → tudo no mesmo PR #382). **(a) `CLINICAL_GUIDELINES` 18→23** (5 entradas novas). **(b) Mapas/flashcards aos alunos** em **"Diabetes"** — `mm_shared` 41→**45** (`mm-dia-tecnologia-ada2026`, `mm-dia-diagnostico-ada2026`, `mm-dia-prevencao-ada2026`, `mm-dia-metas-ada2026`) e `fc_shared` 92→**116** (24 flashcards `fc-dia-{tec,dx,prev,metas}-*`). **O usuário pediu explicitamente: Obesidade SEM mapas/flashcards** (só no `CLINICAL_GUIDELINES`) — os outros 4 viraram mapas/flashcards (confirmado por AskUserQuestion). **Conteúdo-chave:** Diagnóstico = A1C ≥6,5 / jejum ≥126 / 2h ≥200 / aleatória ≥200 c/ sintomas; pré-diabetes 5,7–6,4 / 100–125 / 140–199; estadiamento DM1 (1/2/3 por autoanticorpos). Metas = A1C <7%, TIR >70%, TBR <70 <4% e <54 <1%; hipo Níveis 1/2/3 + regra 15-15 + glucagon p/ todos em insulina; CAD/EHH "sick day" (não suspender basal, cetona sanguínea, CAD euglicêmica, iSGLT2↑risco no DM1). Tecnologia = CGM no diagnóstico, AID = método preferido. Prevenção = DPP (perda ≥5–7% + ≥150 min/sem) + metformina (IMC ≥35, <60a, DMG). **LIÇÃO (render de PDF):** o `Read` de PDF às vezes retorna "PDF pages extracted: N page(s)" SEM renderizar o conteúdo (intermitente, pior em chunks grandes; aconteceu MUITO com o dc26s006). **Solução robusta: `pdftotext -layout <pdf> /tmp/x.txt`** (poppler-utils já vem no container) → extrai o texto e aí uso Grep/Read no `.txt`. Mais confiável que brigar com o render de imagem; usar isso direto para PDFs grandes/problemáticos.
- **Leva de 4 diretrizes/artigos assimilada em LOTE com preview único (#380, 2026-06-18):** o usuário pediu para enviar várias diretrizes/artigos e **fazer um preview só no fim** (modo lote — não publicar item a item). Enviou: (1) **Dislipidemia SBC 2025** (link abccardiol.org bloqueado no egress + WebFetch 403 → li o texto completo via **PubMed Central PMC12674852** com a ferramenta do PubMed; o arquivo de 366k chars foi salvo e busquei por match-only), (2) **IFSO 2024** (medicações antiobesidade + cirurgia bariátrica, BJS), (3) **SURMOUNT-1 3 anos** (tirzepatida, NEJM), (4) **Obesidade sarcopênica ESPEN/EASO 2022** (Obesity Facts) — os 3 últimos por PDF. **Fluxo:** assimilei cada um e **segurei tudo**; no fim montei **1 PR** (`CLINICAL_GUIDELINES` **15→18**, preview enviado) e, após o "pode publicar", **mergeei o #380 + publiquei os mapas/flashcards de uma vez**. **Publicado aos alunos:** `mm_shared` 37→**41** (4 mapas: `mm-lip-dislip-risco-2025` e `mm-lip-dislip-trat-2025` em **Lípides**; `mm-obe-sarcopenica-2022` e `mm-obe-ifso-2024` em **Obesidade**) e `fc_shared` 70→**92** (22 flashcards: 10 `fc-lip-dislip-*` em Lípides; 12 em Obesidade — `fc-obe-sarc-*`, `fc-obe-ifso-*`, `fc-obe-surmount-*`). **Conteúdo-chave:** Dislipidemia 2025 = **nova categoria de risco EXTREMO** (LDL <40); metas LDL muito alto <50 / alto <70 / interm <100 / baixo <115 (não-HDL = +30; ApoB <45/<55/<70); **Lp(a) 1×/vida em todos** (nmol/L); terapia combinada precoce (estatina+ezetimiba±anti-PCSK9; bempedoico/inclisirana); TG ≥500 → fibrato. Obesidade sarcopênica = excesso de adiposidade + baixa massa/função muscular; algoritmo Triagem→Diagnóstico (força + composição corporal)→Estágios I/II. IFSO = suspender GLP-1/tirzepatida antes da cirurgia, pós-op por alvo, medicação antes de revisional. SURMOUNT-1 = tirzepatida ↓ peso ~20% e ↓ progressão p/ DM2 ~93%. **Subespecialidade canônica:** dislipidemia → **"Lípides"** (o `canonSub` mapeia "Dislipidemia e Aterosclerose"→"Lípides"); demais → "Obesidade". **LIÇÃO (egress):** hosts fora da allowlist do egress (ex.: `abccardiol.org`) dão "Host not in allowlist" no curl e 403 no WebFetch — quando for diretriz biomédica publicada, **tentar o PMC via ferramenta do PubMed** (`get_full_text_article`) que funciona.
- **Diretriz Brasileira de Obesidade 2025 (5 sociedades) assimilada, publicada e na IA (#378, 2026-06-18):** o usuário enviou 1 PDF ("Diretriz Brasileira Baseada em Evidências 2025 para o Manejo da Obesidade e Prevenção de DCV", ABESO/SBD/SBC/SBEM/ABS, *Arq Bras Cardiol*) e disse "assimile tudo". Lido via Read (28 págs). É diretriz **nova e distinta** da ABESO 2026 que já tínhamos — foco em **estratificar risco CV na obesidade e tratar por categoria de risco**. (a) **Publicado aos alunos** em **"Obesidade"**: `mm_shared` 35→**37** (`||` 2 mapas: `mm-obe-cv-risco-2025` [estratificação + metas] e `mm-obe-cv-trat-2025` [tratamento por risco]) e `fc_shared` 60→**70** (`||` 10 flashcards `fc-obe-cv-1..10`). (b) **Adicionado ao `CLINICAL_GUIDELINES`** (@~L3171, **14→15**; injetado em toda a IA via `authoringSys`). **Pontos-chave:** avaliar risco DASCV + IC em todo adulto com sobrepeso/obesidade pelo **escore PREVENT** (prevenção 1ª, IMC <40, 30–79a); DASCV baixo <5%/10a, moderado 5–20%, alto >20% (ou DASCV estabelecida, DM2 >10a, DRC 3b, CAC >100/>10, Lp(a) >50, LDL >190); reestratificar moderado por CAC; **risco alto de IC** (IMC>40, AOS grave, FA, DRC 3b, NTproBNP↑). **Metas:** ≥5% (moderado, reduz fatores) e ≥10% (moderado/alto, reduz eventos; também na FA). **Tratamento:** estilo de vida p/ todos; **AR GLP-1/tirzepatida** preferenciais; **semaglutida 2,4 mg** na DASCV estabelecida (SELECT) e na ICFEp; **SGLT2** na IC; **semaglutida 1,0 mg** na DRC (FLOW); **tirzepatida** na apneia (SURMOUNT-OSA, aprovada FDA); **sibutramina contraindicada** no alto risco CV (SCOUT); **cirurgia bariátrica** se IMC ≥35 + risco moderado/alto. **Processo:** "assimile tudo" = autorização direta → publiquei (SQL atômico `||`) + mergeei o #378 (subespecialidade "Obesidade", canônica). **Esclarecimento de contexto:** o usuário perguntou antes se já tinha mandado a "diretriz brasileira de dislipidemia" — esclareci que só temos a **dislipidemia da SBD (no DM)**, não a **geral da SBC**; ele acabou enviando a de obesidade. A da SBC (dislipidemias/aterosclerose) **segue pendente** se ele quiser.
- **+2 diretrizes pediátricas de tireoide assimiladas, publicadas e injetadas na IA (#376, 2026-06-18):** o usuário enviou 2 PDFs ("vou enviar mais diretrizes", mesmo padrão das #368/#369). Lidos via Read (PDF) e (a) **publicados aos alunos** em `endodirect_global_state.payload` (subespecialidade **"Endocrinologia Pediátrica"**, aprovada pelo usuário) — `mm_shared` 33→**35** (`||` 2 mapas novos: `mm-ped-hipocong-2021`, `mm-ped-graves-2022`, com `at`=now() ms) e `fc_shared` 48→**60** (`||` 12 flashcards `fc-ped-hipocong-1..6` + `fc-ped-graves-1..6`); e (b) **adicionados ao `CLINICAL_GUIDELINES`** (@~L3169, **12→14** diretrizes; injetado em questões/chat/OSCE/prescrição via `authoringSys`). **(1) Hipotireoidismo Congênito (Consenso ENDO-ERN/ESPE/ESE 2020-21, *Thyroid*):** triagem por TSH (teste + sensível p/ CH primário; +T4/fT4 p/ central); LT4 imediato se fT4 baixo+TSH alto ou TSH>20 mU/L (não após 2 sem de vida; não atrasar p/ imagem); dose até 15 µg/kg/d (grave fT4<5: 10–15; leve: ~10); **CH central → excluir/tratar insuf. adrenal ANTES do LT4** (risco crise adrenal); reavaliar eixo aos 2–3 anos (glândula in situ; suspender LT4 4–6 sem, TSH≥10=permanente). **(2) Doença de Graves Pediátrica (ETA 2022):** fT4/fT3/TSH + **TRAb em todos** (confirma + prediz remissão) + anti-TPO; **1ª linha carbimazol/metimazol, PTU NÃO** (hepatotoxicidade); preferir titulação de dose (DT) ao bloqueio-substituição; ATD **≥3 anos** (remissão 20–30% em 2 anos; TRAb alto→não parar); **febre/dor de garganta = parar + checar neutrófilos** (<0,5 suspender); definitivo = **tireoidectomia total** (preferida <10a e bócio grande) ou RAI (evitar <5a, preferir >10a); tempestade = iodo+corticoide+betabloq+ATD. **Processo:** apresentei o conteúdo extraído (o médico confirmou a precisão), ele definiu a subespecialidade e disse "pode fazer deploy" → publiquei os mapas/flashcards (SQL atômico, `||`) e mergeei o #376. **Obs.:** 1ª tentativa do SQL deu **502 (gateway transitório)** — verifiquei as contagens (seguiam 33/48) ANTES de reenviar, p/ não duplicar; o reenvio gravou 35/60.
- **Analytics "Conteúdo de estudo": flashcards REVISADOS em vez do baralho inteiro (#373, 2026-06-18):** o card "Conteúdo de estudo dos alunos" mostrava **Flashcards = 887**, mas isso era a soma de TODOS os cards no baralho de cada aluno — que **inclui os flashcards semeados pelo professor** (`seed:true`). Conferido no banco: dos 887, **0 revisados** (`reps>0`) e **0 criados por alunos** (`seed=false`) → "887" passava falsa sensação de engajamento. **Fix backend (migration aditiva `admin_overview_flashcards_revisados`, não quebra produção):** a `endodirect_admin_overview` ganhou `flashcards_revisados` (cards com `reps>0`) e `flashcards_proprios` (cards `seed=false`); o campo antigo `flashcards` (total do baralho) **mantido** p/ compatibilidade. **Fix frontend (@L7491):** o card passa a mostrar **"Flashcards revisados"** = `flashcards_revisados` com contexto " de N no baralho", e **"Mapas criados pelos alunos"** = `mapas` (já era fiel — mapas do professor vivem em `mm_shared`, não em `payload.mm`). Estrutura do card flashcard: `{id,at,cat,front,back,box,ef,due,reps,interval,seed}` — `reps` = nº de revisões SRS (Leitner/SM-2), `seed:true` = semeado pelo professor. **Ordem aditiva (RPC 1º, frontend depois) p/ evitar estado quebrado:** como a RPC é compartilhada por produção e preview (mesmo banco), adicionei o campo novo SEM mexer no antigo → produção seguiu mostrando "887" até o frontend subir, então trocou p/ "0 de 887 no baralho". Aprovado no preview com login e mergeado. **Esclarecimento importante (NÃO era bug):** "Maior acerto / Maior erro / Desempenho por subespecialidade" **já são cumulativos de TODOS os alunos** (a RPC soma o `perf` histórico de cada aluno); aparece "de 1 aluno" só porque, até agora, **apenas arthurms@unicamp.br salvou progresso** (50 respostas, 74% geral). Conforme outros praticarem, entram no agregado automaticamente.
- **Login habilitado em previews da Vercel (#372, 2026-06-18):** o `authBaseURL()` (@L2166) retornava SEMPRE `https://www.endodirect.com.br` (exceto localhost) → OAuth no preview redirecionava p/ produção, tornando o preview impossível de testar logado. Fix: passa a retornar `location.origin` também quando `location.hostname` termina em `.vercel.app`. **Produção inalterada** (www/apex não casam com `.vercel.app`). **Pré-requisito (feito pelo usuário no painel Supabase → Authentication → URL Configuration → Redirect URLs):** adicionar `https://endodirect-*-endodirect.vercel.app/**` (cobre branch alias `endodirect-git-…` e deploys por hash). Sem isso o Supabase recusa o retorno e cai no Site URL (produção). Testado ao vivo: login no preview permanece no preview. Agora previews são testáveis com login (resolve a fricção recorrente). Google Cloud não precisa de mudança (o Google volta p/ o Supabase; quem decide o retorno ao app é a allowlist do Supabase). **LIÇÃO de merge:** PR que toca o cofre + a `main` avança via squash → a branch diverge no mesmo trecho do `Decisões.md` → conflito. Antes de novo PR, `git reset --hard origin/main` e reaplicar só o que falta.
- **Analytics alinhado ao Estudantes: "Alunos cadastrados" = 14 (mesma régua), excluindo conta de teste dos agregados (2026-06-18):** o usuário notou divergência — Estudantes mostrava **14** e Analytics **12**. Causa: fontes diferentes. Estudantes (RPC `endodirect_admin_students`) conta `auth.users` menos professores/teste = 14; Analytics (RPC `endodirect_admin_overview`) contava `app_state role='aluno'` = 12, que **incluía 1 conta de teste** (`memed.teste`) e **excluía 3 alunos reais sem estado salvo** (davijtbj, loharomari, drathaissilvanunes) → +1 −3 = 12. **Fix (migration `align_admin_overview_real_students`):** a `endodirect_admin_overview` ganhou CTEs `admins`/`test_emails`/`registrados` (cadastrados = `auth.users` menos professores/teste, igual ao painel Estudantes) e o CTE `alunos` (base de atividade) passou a **excluir e-mails de professor e de teste**. Agora `'alunos'` (cadastrados) conta de `registrados` = **14**; perf/sf/geo/ativos não contam mais a conta de teste. "Alunos com atividade" segue **1** (o ativo é aluno real). RPC aplicada direto no banco (efeito imediato; basta o professor recarregar o Analytics). **Régua única "aluno real" = `auth.users` menos `endodirect_admins` menos [`aluno@`,`alunopro@`,`memed.teste@`]** — usada pelas DUAS RPCs admin.
- **Painel "Estudantes" do professor agora lista os ALUNOS REAIS do backend (#370, 2026-06-18):** o usuário reportou que os alunos cadastrados não apareciam no painel — só um aluno fake ("Aluno Endodirect"). Causa: `admEstudantesHTML` lia de `admEstudantes` (array em localStorage, semeado com 1 fake), nunca consultava `auth.users`. **Fix backend (sem nova função serverless — `api/` está em 12/12):** nova **RPC `endodirect_admin_students()`** no Supabase (`SECURITY DEFINER`, owner `postgres`, mesmo padrão admin-gated da `endodirect_admin_overview`): valida o professor por `endodirect_admins` (e-mail do JWT) e devolve os alunos de `auth.users`, **excluindo os 5 professores + contas de teste** (`aluno@`, `alunopro@`, `memed.teste@`), cruzando com `endodirect_acessos` (`scope='plano:%'`, `status='active'`, não expirado) p/ o **plano** de cada um. Grants = `authenticated/anon/service_role` (igual à overview). **Fix frontend:** o painel busca via `client.rpc('endodirect_admin_students')` (mesmo padrão async do Analytics: loading/erro/retry), mostra **Total/Ativos/Assinantes/Este mês** e por aluno **nome, e-mail, plano (Gold/Standard/Degustação), cadastro, último acesso e status** (Ativo / "Sem 1º acesso"). Removido o protótipo (lista fake, "+ Cadastrar", aviso de sessão). `admEstudantes`/`persistEstudantes` mantidos só p/ a camada de sync (`adm_estudantes` em REMOTE_STATE_KEYS). Resultado: **14 alunos reais** (2 Gold: suzanne, ao_telles; 1 sem 1º acesso: davijtbj). **LIÇÃO — preview não é testável com login:** `authBaseURL()` (@L2166) retorna SEMPRE `https://www.endodirect.com.br` (exceto localhost), então OAuth no preview redireciona p/ produção → o usuário não conseguia ver a mudança no preview. Como a RPC já estava no banco e testada, a mudança é isolada ao painel do professor (não toca login/boot) e o CI passou, o usuário optou por **publicar e conferir ao vivo** (rollback pronto). Para tornar previews testáveis com login no futuro: fazer `authBaseURL()` usar `location.origin` em `*.vercel.app` **E** adicionar o wildcard de preview em Supabase → Auth → URL Configuration → Redirect URLs (senão o Supabase recusa e cai no Site URL=produção). Ver [[Convenções de Trabalho]]. o usuário enviou 3 PDFs pediátricos p/ "treinar/atualizar". Assimiladas via `pdftotext` e (a) **adicionadas ao `CLINICAL_GUIDELINES`** (agora **12 diretrizes**; ver entrada abaixo — folded no #368) e (b) **publicadas aos alunos** em `endodirect_global_state.payload` (`mm_shared` `||` 3 mapas, `fc_shared` `||` 16 flashcards), todos em **"Endocrinologia Pediátrica"** (subespecialidade canônica, sem remap no `canonSub`). **(1) Obesidade pediátrica (AAP 2023):** mapa `mm-ped-obe-2023` (obesidade = IMC ≥P95; grave classe 2 ≥120% P95 ou IMC ≥35, classe 3 ≥140% P95 ou IMC ≥40; **sem conduta expectante**; base = IHBLT ≥26h; farmacoterapia ≥12a, cirurgia ≥13a) + 6 flashcards `fc-ped-obe-1..6`. **(2) PIG/SGA (Consenso Internacional 2023):** mapa `mm-ped-pig-2023` (PIG = peso/comprimento <-2 DP ao nascer; ~10% sem catch-up; GH se sem catch-up aos 3–4a e estatura <-2,5 DP; considerar +GnRHa; risco metabólico) + 5 flashcards `fc-ped-pig-1..5`. **(3) GH de ação prolongada (consenso JCEM 2025):** mapa `mm-ped-gh-2025` (lonapegsomatropina/somapacitana/somatrogon semanais, não inferiores ao diário; dose por peso; IGF-1 respeitando o tempo desde a aplicação) + 5 flashcards `fc-ped-gh-1..5`. Mapas usam `at` = `now()` em ms (recência). **Processo:** o usuário deu instrução direta ("crie mapas+flashcards … em seguida pode dar deploy") → publiquei o conteúdo e mergeei o #368. Resultado: `mm_shared` 30→33, `fc_shared` 32→48.
- **Diretrizes centralizadas em `CLINICAL_GUIDELINES` e injetadas em TODOS os geradores de IA (2026-06-18, pedido do usuário):** o usuário pediu que as diretrizes assimiladas valham não só p/ mapas/flashcards, mas p/ tudo que a IA gera (questões, chat, prescrição, OSCE). Resolvido o problema de escala que eu tinha sinalizado: criei **um bloco único `var CLINICAL_GUIDELINES`** (`@~L3150`, logo após `CLINICAL_AUTHORING`) com as **9 diretrizes** (DRD, dislipidemia, gestação, ATA tireoide, ABESO obesidade, hipogonadismo SBEM, hiperaldosteronismo ES 2025, incidentaloma adrenal ESE 2023, incidentaloma hipofisário Pituitary 2025) + terminologia. Removidas as regras 5-8 do `CLINICAL_AUTHORING` (agora vivem no bloco). **`authoringSys` passa a incluir `CLINICAL_GUIDELINES`** (questões/flashcards/mapas) e o bloco foi **concatenado** em: Chat IA (2 ocorrências `sendChat`), OSCE (geração `@~L8799` + relatório `@~L8888`), Prescrição Simulada (gerador de caso `@~L8902` + avaliação `@~L8926`). **Fonte única:** ao assimilar diretriz nova, atualizar SÓ o `CLINICAL_GUIDELINES`. Ainda **fora** (se o usuário quiser depois): "Prescrição Comentada"/Consultório (`@~L5786/6028`), feedback passo-a-passo do OSCE (`@~L8832`) e o gerador de caso curto (`@~L5380`). Mudança de frontend → **PR + preview + aprovação** antes do merge (regra reforçada). Ver [[Banco de Questões]].
- **Incidentalomas adrenal (ESE 2023, `lvad066`) e hipofisário (Pituitary Society 2025, `s41574-025-01134-8`) publicados (2026-06-18, aprovado pelo usuário):** 2 mapas NOVOS + 10 flashcards via `endodirect_global_state.payload`. **(a) Incidentaloma Adrenal (Adrenal):** mapa `mm-inc-adrenal-2023` (TC sem contraste **HU ≤10 = benigno**, sem mais imagem; ≥4 cm heterogêneo ou HU >20 → cirurgia; hormonal em todos: dexa 1 mg ≤1,8 µg/dL + metanefrinas + ARR se HAS/K↓; **MACS** = novo termo p/ "Cushing subclínico", rastrear comorbidades; benigno → sem seguimento de imagem) + 5 flashcards `fc-adr-inc-1..5`. **(b) Incidentaloma Hipofisário (Neuroendocrinologia):** mapa `mm-inc-hipofise-2025` (micro <10mm vs macro ≥10mm; maioria não funcionante; rastrear hipersecreção + hipopituitarismo; **campo visual se tocar/comprimir o quiasma**; operar se compressão visual/apoplexia/hipersecreção; micro → observação + RM de vigilância; prolactinoma → clínico) + 5 flashcards `fc-neu-inc-1..5`. Mapas NOVOS = `||` (append) em `mm_shared`. **Processo:** apresentei e o usuário aprovou ANTES de publicar.
- **Hiperaldosteronismo primário (Endocrine Society 2025, JCEM `dgaf284`) publicado em "Adrenal" (2026-06-18, aprovado pelo usuário):** assimilada via `pdftotext`. No `endodirect_global_state.payload`: (a) mapa `mm-1780536769131` "Hiperaldosteronismo Primário" **atualizado** → root "...(Endocrine Society 2025)", ramos Quadro/fisiopatologia · Rastreio (ARR, mais amplo) · Confirmação e subtipagem (TC + AVS; dexa 1 mg) · Tratamento (adrenalectomia vs MRA); (b) **7 flashcards** `fc-adr-pa-1..7` em "Adrenal". Pontos da diretriz: **rastreio ampliado** (ARR em hipertensos em geral — maioria normocalêmica, derruba o mito da hipocalemia); pode **dispensar confirmação** se hipocalemia espontânea + renina suprimida + aldosterona alta; subtipagem por **TC + cateterismo de veias adrenais (AVS)**; unilateral → **adrenalectomia**; bilateral → **MRA** (espironolactona preferida), titulado pela renina. **Processo:** apresentei o conteúdo e o usuário aprovou ANTES de publicar (regra do preview p/ conteúdo de aluno). **Obs.:** o usuário disse "duas diretrizes" mas só 1 PDF chegou — pedir o 2º.
- **Hipogonadismo masculino (SBEM/SBU/ABEMSS 2026) publicado em "Endocrinologia Masculina" (2026-06-18, aprovado pelo usuário):** diretriz assimilada via `pdftotext`. Publicado no `endodirect_global_state.payload` via SQL: (a) mapa antigo `mm-1780536769142` ("Hipogonadismo Masculino") **movido de "Endocrinologia Feminina e Andrologia" → "Endocrinologia Masculina"** + `data` atualizada como **Diagnóstico** (TT jejum/manhã/2 dias; cortes **<264 apoia / >350 exclui / 264–350 zona cinzenta → T livre**; testosterona livre se SHBG alterada; classificação primário/secundário/funcional/compensado por LH/FSH) — **corrige a má-categorização** (estava em Feminina via `canonSub`); (b) novo mapa `mm-hipo-trt-2026` **"Reposição de Testosterona (TRT)"** (indicação no orgânico sintomático; IM-LA/transdérmica; contraindicações: CA próstata/mama, AOS grave, Ht>54%, IC NYHA III/IV, fertilidade; monitorização 3/6/12m + Ht/PSA; fertilidade → SERM/clomifeno, hCG, IA); (c) **8 flashcards** `fc-mas-hipo-1..8` em "Endocrinologia Masculina". Subespecialidade canônica = "Endocrinologia Masculina" (dropdowns/FC_CATS). **Sem incorporar no CLINICAL_AUTHORING desta vez** (prompt já com 4 diretrizes, regras 5-8; o entregável pedido foram mapas/flashcards). Padrão de publicação: mapa = editar elemento em `mm_shared` por id (ou `||` p/ novo); flashcards = `||` em `fc_shared` (seeding c/ ledger, seguro).
- **Chat IA restrito a temas médicos (2026-06-17):** pedido p/ impedir o aluno de usar o Chat IA para assuntos aleatórios (viagens, etc.). Prefixada uma trava de **ESCOPO RESTRITO** no system prompt do chat (responde só medicina/endocrinologia/estudo clínico; fora disso, recusa em 1 frase e não responde). Aplicada nas **2 ocorrências** do prompt (`sendChat` @~L6760 e a cópia viva reatribuída @~L8231 — mesmo padrão de duplicação morto/vivo do resto do app). Limitação conhecida: trava por prompt não é 100% à prova de jailbreak, mas resolve o uso casual (que é o objetivo). Se precisar de reforço, dá p/ pré-filtrar no cliente/servidor por classificação de tópico.
- **Diretrizes SBD incorporadas aos prompts de IA — terminologia + metas (2026-06-17):** o usuário vem enviando PDFs de diretrizes da SBD p/ manter o conteúdo gerado atualizado ("inserir nos comentários quando julgar necessário"). Já incorporados em `CLINICAL_AUTHORING` (prompt-base de questões/flashcards/mapas) + diretrizes de terminologia espalhadas nos prompts de OSCE (geração/relatório), gerador de caso da prescrição e avaliação de prescrição: **(1) "Manejo da doença renal do diabetes"** → usar "doença renal do diabetes" (não "nefropatia diabética") e "albuminúria"/RAC (não micro/macroalbuminúria); rastreio por RAC+TFGe; renoproteção IECA/BRA + iSGLT2 + finerenona. **(2) "Manejo do risco cardiovascular: dislipidemia"** → metas de LDL-c por risco CV no DM: muito alto <50, alto <70, intermediário/baixo <100 mg/dL (não-HDL = LDL+30); estatina alta intensidade → ezetimiba → iPCSK9; Lp(a) ≥50 mg/dL agravante. **(3) "Tratamento farmacológico do diabetes na gestação"** → insulina é 1ª linha; metformina só como alternativa se insulina inviável; metas jejum <95, 1h <140, 2h <120 mg/dL. **(4) ATA 2026 "Thyroid disease in preconception, pregnancy and postpartum" (regra 7 do CLINICAL_AUTHORING)** → TSH 1º trimestre limite superior ~4,0 mU/L (≈0,5 abaixo do não-gestante; específico do ensaio/população, NÃO o antigo "<2,5/3,0" fixo); quem usa LT4 mira TSH 0,5–2,5 na pré-concepção e aumenta a dose ~20–30% ao confirmar gravidez; HSC: considerar LT4 se 1º trimestre, NÃO de rotina após; eutireoidiana TPOAb+: LT4 NÃO de rotina (TABLET/T4LIFE negativos); iodo 150 µg/dia; hipertireoidismo PTU no 1º trimestre e metimazol depois; sem rastreio universal. **(5) ABESO 2026 "Tratamento farmacológico da obesidade" (regra 8)** → farmacoterapia se IMC ≥30 ou ≥27 com complicação relacionada à adiposidade; meta de perda ≥10% (5-10% já beneficia); maior eficácia tirzepatida (~20-25%) e semaglutida (~15%) > liraglutida; outras orlistate, naltrexona/bupropiona; **sibutramina NÃO em DCV aterosclerótica (SCOUT)**; tratamento crônico/longo prazo (suspender→reganho); atenção à sarcopenia (proteína + treino de força); NÃO usar fórmulas magistrais. **Verificação com `pdftotext` (após instalar poppler) 2026-06-18:** dislipidemia/ATA/DRD conferidos OK; corrigida a meta da **gestação** — a diretriz de *tratamento* enfatiza jejum <95 e 2h <120 (o "1h <140" é da automonitorização padrão SBD, não desta PDF) → regra 6 ajustada. Texto fixo "Microalbuminúria" (exame da prescrição) → "Albuminúria". **Critério:** metas estáveis e de alto rendimento entram no prompt-base (CLINICAL_AUTHORING, regras 5-8); terminologia entra como diretriz curta em cada prompt clínico. PDFs ficam só como referência (não versionados). **Escala:** o CLINICAL_AUTHORING já tem 4 diretrizes (regras 5-8) — quando vierem muitas mais, mover p/ um bloco de "diretrizes recentes" injetado por tema, p/ não inchar todo prompt. **Conteúdo de obesidade publicado a todos os alunos (2026-06-18, aprovado pelo usuário após revisão):** via SQL no `endodirect_global_state.payload` — (a) **mapa** `mm_shared` id `mm-1780536769128` atualizado de "Farmacoterapia da Obesidade" → **"Farmacoterapia da Obesidade (ABESO 2026)"** (ramos: Indicação / Incretinomiméticos / Outras opções / Princípios); (b) **7 flashcards** publicados em `fc_shared` (ids `fc-obe-abeso-1..7`, `sub:"Obesidade"`). **Seguro:** o seeding de `fc_shared` (`@~L2361`) tem **ledger próprio** (`fc_shared_seeded`, idempotente, respeita deleção) — ≠ do seeding de mapas que causou duplicata (#348); mapas do professor são exibidos direto de `sharedMM` (sem seeding). Para futuros flashcards do professor, append em `fc_shared`; para mapas, editar o elemento em `mm_shared` por id. Ver [[Banco de Questões]], [[Integrações]].
- **UI: botão "Entrar" do login mais claro + chip "Gold" dourado (2026-06-16):** (1) `.login-btn` usava `background:var(--navy)` (#111d35), quase invisível no card escuro do login → trocado por gradiente azul da marca `linear-gradient(135deg,#3b6fd4,#5585e8)` + sombra + hover. (2) O chip de plano da topbar (`#tb-plan`, `updatePlanBadge`) pintava qualquer plano de azul; agora o **Gold** ganha visual dourado premium (gradiente `#fde68a→#f5b32c`, texto `#5a3e00`, borda âmbar); Standard segue azul e Degustação cinza (todos os ramos setam `fontWeight`/`border` p/ não vazar estilo entre estados). CSS/visual.
- **Newsletter "saía desatualizada" = ranking por TIPO antes de recência + data quebrada (2026-06-16):** a newsletter diária ("3 artigos do dia") liderava com artigo de dias atrás (ex.: diretriz Endocrine Society de 13/06 num e-mail de 16/06). Causa em `lib/newsletter.js` `rankArticles`: (1) o candidato é `topArticles` do dia **+ o mural INTEIRO (~30 dias, 146 itens)** re-ranqueado; (2) a ordenação começava por **TIPO** (revisão/diretriz tier 0 sempre acima de original/ensaio), então uma diretriz de semanas atrás vencia artigos novos; (3) o desempate por data usava `Date.parse("15/06/2026")` que dá **NaN** no formato dd/mm/aaaa → recência virava no-op. Fix: ranking **recência-primeiro** — novo `artDateMs()` (parser robusto: dd/mm/aaaa, "aaaa Mon dd", "Mon aaaa", mm/aaaa, aaaa; fallback p/ o `at` de entrada no mural) + `freshnessBucket()` (≤2d, 3-4d, 5-9d, 10-20d, +; **data de issue futura tipo "07/2026" → faixa 1**, recente mas não acima do que saiu hoje). `rankArticles` agora ordena por **frescor → tipo → revista → data exata**. Vale p/ a newsletter diária E a de teste (ambas usam `rankArticles`). Mudança **server-side** (lib/, validada por `node --check`; sem risco de frontend); verificável pelo botão "newsletter de teste" do painel admin. Lógica conferida com simulação Node usando os artigos reais de hoje (originais de 16/06 lideram; diretriz de 13/06 sai do top 3).
- **Flashcards: ocultar os 240 cards na entrada; aparecem ao clicar na categoria (#354, 2026-06-16):** a aba Flashcards abria com os 240 cards (filtro default `fcFilter=''` = "Todos"). Pedido: esconder na entrada, mostrar só ao clicar num botão de categoria. Fix: `fcFilter` default `''`→**`null`** (= "nada selecionado", distinto de `''`="Todos"); `renderFC` mostra hint "Escolha uma categoria…" quando `fcFilter==null` (sem cards). Clicar em qualquer pílula (`data-fcfilter`, inclusive "Todos") seta `fcFilter` não-nulo → cards aparecem. Para create/generate não cair no hint, `saveFC` seta `fcFilter=fcSelectedCat` e o `genFC` vivo (@L8651) seta `fcFilter=sub` antes do `renderFC`. Mesmo padrão "lista oculta até escolher" dos Cursos (#352) e Mapas. CSS/render only, sem risco de boot.
- **OSCE: erro na 1ª tentativa = "4 a 6 passos" voltou no revert; fix = 4 passos + feedback/conclusão ricos (#353, 2026-06-16):** o gerador vivo (`startSim` @L8759) pedia **"4 a 6 passos"** com `maxTokens 2600` → caso de 5-6 passos **truncava** o JSON → `parseJ` falhava na largada; só ia num retry que gerasse 4 passos ("sempre erra na 1ª, na 2ª vai"). Era o fix documentado (cap em 4 passos) **perdido no revert do apagão** p/ `b9936dc`. Reaplicado: **EXATAMENTE 4 passos**. Pedido do usuário: manter 4 passos MAS o **feedback/conclusão final detalhados** ("muito importante"). Então: (a) feedback por passo voltou a ser rico (**2-4 frases** citando alvo/dose/diretriz), sem o cap de "1-2 frases"; (b) `maxTokens` geração 2600→**3200** + `withTimeout` 50→**56s** (folga p/ 4 passos com feedback rico sem truncar/cold-start, abaixo do `aiRequest`/server 60s); (c) a **conclusão final já existia** — `finishSim` (@L8848) faz 2ª chamada (Opus, `AI_MODEL_CLINICO`) gerando relatório personalizado (nota, veredito, fortes/fracos, raciocínio, conduta/doses, conclusão, estudar) com base nas respostas do aluno; reforçada p/ ser mais detalhada (raciocínio/conduta 3-5 frases; conclusão = diagnóstico + conduta ideal + mensagens de aprendizado 4-6 frases) + `maxTokens` 1900→**2400**. **LIÇÃO:** o cap de passos (estrutura) é o que evita truncar/504, NÃO o tamanho do feedback — então dá p/ ter 4 passos E feedback rico, só ajustando `maxTokens`/`withTimeout` ao orçamento de tempo. Teste real = abrir OSCE no preview e gerar 2-3× (tem que ir de 1ª).
- **Cursos com nav em 2 níveis + fix do "×" das questões + 3ª aba "Banco Salvo" (#352, 2026-06-16):** 3 pedidos com prints. (1) **Cursos:** na entrada apareciam os 4 cards de curso **E** a grade de subespecialidades junta. Agora `renderCursosAluno` faz **early-return** quando `!cursoFilter` (mostra só os cards de curso + hint); as subespecialidades (módulos) só renderizam **após escolher um curso**. Clicar no card seta `cursoFilter` (handler já existente, L8323, toggla); clicar de novo no card ativo volta p/ só-cursos. (2) **"×" não apagava questão salva:** o botão `data-del-q` fica **dentro** do `.q-row` clicável; o handler do `.q-row` (L8289) rodava 1º e dava `return` → só expandia. Fix = guarda `if(qrow&&!c('[data-del-q]'))` (mesmo padrão do `data-fcdel`). (3) **Aba Questões reorganizada (pedido do usuário):** toggle agora tem **3 botões na ordem `Banco de Questões` (esquerda, default) · `Geradas por IA` · `Banco Salvo`**. "Banco Salvo" é nova aba (`data-qmode="salvo"` → `#q-panel-salvo`) que guarda as questões geradas por IA que o aluno salvou **+** as do Resumidor (é o `#q-saved-list`/`DB.q`, só movido p/ aba própria). Handler do `qmode` virou 3-modos (provas/ia/salvo); `q-gen-results` (compartilhado por geração IA e banco de provas) é escondido na aba salvo; `applyQuestDegustacao` também esconde o painel salvo. Default visível trocado de `ia`→`provas` no HTML. As 9 questões de teste que sobraram são localStorage da conta demo `alunopro` — o fix do "×" deixa o aluno apagá-las. (4) **Chat IA — ordem dos critérios de DM:** a tabela "Critérios Diagnósticos de DM2" é **gerada pela IA** seguindo a ordem do system prompt, que listava o TOTG-2h antes do TOTG-1h → reordenado o prompt (2 ocorrências: chat send L8212 + suggestion L6741) p/ **jejum → TOTG-1h → TOTG-2h → HbA1c → casual**, com diretriz explícita "TOTG-1h LOGO ACIMA do TOTG-2h". **Processo:** PR + preview + OK do usuário antes do merge (pedido fixo).
- **Resumidor (markdown + responder inline) + nav escura + auto-saneamento de mapas — #350 (2026-06-16):** 4 ajustes reportados com prints. (1) **Simulado/NAVEGAÇÃO escura no tema escuro:** `.sf-nav-btn` usava `background:var(--surface)` (quase invisível no escuro) → trocado por `background:var(--bd);color:var(--tx)` + hover azul. CSS puro. (2) **"Gerando…" duplicado no Resumidor:** `resumeArt` punha `ldHTML()` no **botão E** no painel → mantido só o loader do painel; botão fica desabilitado com rótulo estático "Gerando…" (mesmo padrão do "Avaliando" da Prescrição). (3) **Resumidor mostrava `###`/`**` crus + aluno não achava as questões geradas:** o resumo agora renderiza com `mdToHtml(d.resumo)` (classe `.art-md` p/ títulos/listas/tabelas) em vez de `esc(...)`+`pre-wrap`; e as questões/flashcards gerados agora são **respondíveis no próprio painel** (`artQuizHTML` + `wireArtResult` — handler delegado anexado 1× ao `#art-result`, lê `artLastData`; clica alternativa → `.correct`/`.wrong` + explicação; flashcard "Mostrar resposta"). Continuam salvos em `DB.q`/`DB.fc` (Banco/Flashcards/Revisão). (4) **Duplicatas de mapas "ainda apareciam":** o **servidor já estava limpo** (SQL confirmou `mm`=`[]` p/ os 10 users, inclusive endodirectmaster) — o que sobrava era **resíduo de localStorage/aba antiga no navegador**. Adicionado **auto-saneamento** em `refreshAfterRemoteState` (`@~L2721`, dentro do `try/catch`): `DB.mm=DB.mm.filter(m=>!(m&&m.seed))` + `lsSet('mm')` + `queueRemoteStateSave()` se mudou → qualquer cliente no código novo limpa sozinho o resíduo e re-salva limpo (mapas do aluno, sem `seed`, ficam). **PROCESSO:** como é **JS de frontend**, subi em PR (#350), deploy de **preview** (`endodirect-git-…vercel.app`), e **só mergeei após o usuário confirmar no preview** (ele 1º testou produção por engano — "tá tudo igual" — pois os fixes estavam só no preview; expliquei que o preview é outra URL). Risco baixo confirmado: única coisa no caminho de login é o filtro acima, dentro de `try/catch`; resto é CSS ou roda só na tela do Resumidor. Ver [[Convenções de Trabalho]].
- **Mapas: duplicata persistia na conta DEMO `alunopro` — filtro no RENDER (#351, 2026-06-16):** depois do #350, o aluno ainda via a duplicata. Causa: a conta testada era **`alunopro@endodirect.com.br`** (demo local, "Aluno Premium"), que **não tem `currentUser.id`** (linha "Conta demo (sem id remoto)") → o **prune do `refreshAfterRemoteState` (gated em `currentUser.id`) NÃO roda** p/ ela, e o mapa semeado fica preso no localStorage (não está no Supabase, então a limpeza SQL e o replace do `applyStatePayload` também não o tocam). **Fix definitivo = filtrar `seed:true` no RENDER** (`mmSubGrid` count + `renderMMList` lista/contagem), garantindo que mapa semeado **nunca aparece** p/ nenhuma conta (demo ou real), independente de cache/prune/localStorage. **LIÇÃO:** correção de dados (SQL) + prune no boot não cobrem **contas demo locais** nem localStorage residual de abas; quando o objetivo é "nunca mostrar X", filtrar no **render** é a trava à prova de bala. **PROCESSO (pedido do usuário 2026-06-16):** **sempre mandar o link do PREVIEW no chat e esperar o OK** antes de mergear (não auto-mergear frontend). Ver [[Convenções de Trabalho]].
- **Mapas mentais: parar de semear cópias pessoais (remove duplicatas) — #348 (2026-06-16):** nos temas (Diabetes, Tireoide, Adrenal, Obesidade, Lípides e nas demais subespecialidades) os mapas apareciam **duplicados**: o do professor (tag "Professor", sem "×", vindo de `mm_shared`) e uma **cópia pessoal** semeada (`seed:true`, com "×"). Causa: `seedMindmaps()` (`@~L4843`) semeava cópias pessoais a partir do array `MM_SEED`; como o professor passou a publicar um conjunto curado completo via `mm_shared` (27 mapas, visíveis a todos **inclusive degustação** — `renderMMList` não filtra `sharedMM` por membership), essas cópias viraram duplicatas. Clicar no "×" **não resolvia**: `seedMindmaps()` roda a cada login (`@~L2721`, dentro de `try/catch`) e **readicionava** os mapas — idempotente só por tema presente em `DB.mm`, **sem ledger de exclusão**. **Fix em 2 partes:** (1) **código** — `seedMindmaps()` virou **no-op** (`return 0;`), mudança mínima pós-boot numa função-folha já protegida por `try/catch` (não pode derrubar load/login; ≠ dos 2 apagãos, que foram estruturais: `document.write` no head e restruturação async do OSCE). `MM_SEED` fica no código só como referência histórica. (2) **dados** — após o deploy ficar **live em produção** (confirmado: comentário "Seeding…DESATIVADO" presente, loop antigo ausente), `UPDATE` em `endodirect_app_state` removendo elementos `seed:true` de `payload->'mm'` (filtra com `jsonb_agg` preservando qualquer mapa não-seed). Diagnóstico-chave por SQL antes de mexer: **94 mapas pessoais em 10 usuários, TODOS `seed:true` — ZERO criados pelo usuário** → limpeza não perde nada; mapas reais seguem vindo de `mm_shared`. Resultado: 10 users → `mm`=`[]`, 0 seeds restantes. **Ordem importa:** deployar o no-op ANTES da limpeza (senão re-semeiam no próximo login). `applyStatePayload` (`@~L2660`) faz **replace** de `DB.mm` (não union-merge), então load novo = dado limpo, durável. Único resíduo possível: aba antiga (código velho) que salve por cima — auto-cura no reload; reexecutar o `UPDATE` se aparecer straggler.
- **2º APAGÃO: o OSCE lazy TAMBÉM derruba o navegador real — abandonado (2026-06-16):** reapliquei o OSCE lazy (#345, = `index.html` do #338 sem os `document.write`), achando que a causa do 1º apagão fora só o `document.write`. **Quebrou de novo** — usuário travado fora (landing logada-fora, nenhum botão). Revertido no #346 (`index.html` ao estável `b9936dc`). **Conclusão crítica:** o OSCE lazy, sozinho, **também** derruba a interatividade em navegador real — e `ci-validate` + sandbox `vm` (rodando TODOS os scripts inline head+body) **passaram mesmo assim**. Ou seja, **meu ferramental não valida mudança de JS desse `index.html` gigante** (um erro de runtime no load derruba tudo e não é reproduzível daqui). Mecanismo exato do lazy desconhecido (só defs de função no body). **DECISÃO:** OSCE fica na **forma de geração única** (estável); **não reaplicar lazy**. **PROCESSO obrigatório p/ JS no frontend:** subir na branch e **testar o deploy de PREVIEW da Vercel do PR em navegador real ANTES de mergear** — produção nunca quebra. CSS simples pode ir direto. Ver [[Pendências]].
- **APAGÃO de interatividade causado por `document.write` no `<head>` — REVERT p/ #334 (2026-06-16):** sequência real (corrige a tese ERRADA da nota anterior). (1) Usuário "não entrava". Diagnostiquei **errado** como CDN do Supabase (single point of failure) e adicionei fallbacks `window.supabase||document.write('<script src=unpkg/statically…>')` no `<head>` (#339/#341). (2) **Piorou:** usuário reportou que **NENHUM botão da landing funcionava, nem em navegador novo**. Navegador novo falhando = é **CÓDIGO**, não cache/rede; e o **4G já tinha falhado** = não era rede/CDN (minha tese estava errada o tempo todo). (3) **Causa real:** os `<script>` de fallback com **`document.write` no `<head>` quebraram a execução do script principal em navegador real** → o `DOMContentLoaded` (~L8936) que registra TODOS os handlers de botão + `startSupabaseAuthWatcher` nunca roda → app morto, **nenhuma** requisição de login chega ao Supabase. O teste de sandbox `vm` deu **FALSO OK** porque só executou o `<script>` gigante (L1937-9268), **não os scripts do `<head>` (L742+)**. (4) **Fix: `git checkout f78bb86 -- index.html`** (restaura o `index.html` EXATO do #334, onde login funcionava às 02:09; `git diff f78bb86 -- index.html` = vazio) → #342. **Confirmado nos logs:** depois de **7h sem nenhuma requisição de auth** (02:09→10:43), logo após o revert o Supabase voltou a receber `/token` (10:43) = **app vivo de novo**.
- **LIÇÕES (caras) do apagão (2026-06-16):** (a) **NUNCA usar `document.write` para fallback de `<script>`** — quebra o parse/execução do documento em navegador real. Fallback de CDN, se precisar, só com `onerror`/carregamento programático **testado em navegador de verdade**. (b) O teste de sandbox `vm` tem que incluir **TODOS os `<script>` (head + body)**, senão dá falso OK (foi o que aconteceu). (c) `ci-validate` só faz **parse** (`new Function`), não pega erro de runtime no load. (d) Usuário trancado fora + **falha em navegador novo** = é código → **reverter pro último estado bom IMEDIATAMENTE** (`git checkout <sha-bom> -- index.html`), diagnosticar depois. (e) 4G/outra rede falhando já descarta rede/CDN — não insistir nessa hipótese. Ver [[Convenções de Trabalho]].
- **OSCE: geração PASSO A PASSO (lazy) — casos de 4-5 passos sem 504, largada rápida (2026-06-16):** em vez de gerar o caso inteiro numa chamada (grande/lenta → 504 ou truncamento), o OSCE agora gera sob demanda. `startSim` traz só `{intro, step1}` (chamada pequena, ~6-10s na largada). Cada passo seguinte é gerado por `advanceSim()` (novo) com o **contexto acumulado** (vinheta + passos/respostas anteriores), chamada `callAIJson(...,1100,...)` pequena → ~4-6s, sem estourar a função. Teto `SIM_MAX_STEPS=5` (client cap; `advanceSim` chama `finishSim` ao atingir). `selectSimOpt`/`submitSimOpen` chamam `advanceSim` em vez de ler um passo pré-gerado; `updateSimCounter` usa `maxSteps` como total; `finishSim` usa `answers.length`. Em erro de um passo: botão "⟳ Tentar de novo" / "Finalizar agora" (não trava a sessão). Mantém Sonnet + rigor clínico. Lógica do state-machine (contagem/cap/sem-loop) validada por simulação Node. **REVERTIDO no #342 junto com o apagão — REAPLICAR com teste em navegador real (ver Pendências).** Padrão reaproveitável: conteúdo multi-item revelado em sequência (passos) deve ser gerado item-a-item. Ver [[Convenções de Trabalho]].
- **OSCE confiável = limitar o TAMANHO do caso (4 passos), não tunar maxTokens/tempo (2026-06-16):** depois de muito vai-e-vem, os logs da Vercel fecharam o diagnóstico. O caso rico de **4-6 passos é grande/lento demais pro Sonnet**: com `maxTokens` baixo (2600) **trunca** (200 com `stop_reason:max_tokens` → `parseJ` falha → "não iniciou"); com `maxTokens` alto (4000) o modelo gera ~4000 tokens em ~60s e **estoura `maxDuration` → 504 Vercel Runtime Timeout** ("A geração demorou demais"). Ou seja, mexer em maxTokens/maxDuration só troca um modo de falha pelo outro. **Solução: bound no tamanho do caso** — manter Sonnet + todo o rigor clínico (labs, valores, doses, diretrizes), mas **EXATAMENTE 4 passos** + feedback de 1-2 frases → caso ~1300-1600 tokens → gera em ~15-25s, sem truncar e sem 504. `maxTokens` 4000→**2400** (cap de segurança: pior caso ~35s < 60s), `withTimeout` 70→45s. **LIÇÃO: para LLM não-streaming com teto de função, o tamanho-alvo da saída tem que caber no orçamento de TEMPO do modelo escolhido; controlar isso pelo PROMPT (nº de itens), não só pelo maxTokens.** `maxDuration` segue 60 (cobre prescrição em Opus). Diagnóstico via `mcp__Vercel__get_runtime_logs` (filtrar `source=serverless`; janela curta — o fetch de logs dá timeout em janelas largas).
- **OSCE "não iniciava" = JSON truncado, não timeout (2026-06-16):** usuário relatava que o OSCE não gerava o caso. Diagnóstico pelos **logs de runtime da Vercel** (MCP `mcp__Vercel__get_runtime_logs`, projeto `prj_2GxMHuvw0UCAu1OWN4RacOrAA8OP`, team `team_fufkQHFICWnQDbeIKmAKo6a8`): **todas as chamadas `/api/ai` retornavam 200, zero erros/5xx**. Logo o servidor estava OK — a falha era no cliente: o caso rico (Sonnet, prompt detalhado, 4-6 passos) **estourava `maxTokens=2600`** → a Anthropic devolve 200 com `stop_reason:max_tokens` e JSON incompleto → `parseJ` falha → as 2 tentativas falham → toast de erro (que some) → botão volta ao normal = "não iniciou". **LIÇÃO: 200 no `/api/ai` mas o recurso não funciona ⇒ suspeitar de truncamento por `maxTokens`, não de timeout; conferir os logs da Vercel antes de chutar.** Fix sem mexer na qualidade do prompt: OSCE `maxTokens` 2600→**4000** (teto do `clampTokens`, o caso completa o JSON); `api/ai.js` `maxDuration` 30→**60** + reforço autoritativo em `vercel.json` (`functions.api/ai.js.maxDuration=60`) p/ a geração maior ter tempo; tetos do cliente subiram junto (OSCE `withTimeout` 50→70s, `aiRequest` AbortController 60→90s). Ver [[Convenções de Trabalho]].
- **Merge + deploy automáticos pré-autorizados (2026-06-16):** o usuário autorizou em definitivo mergear (squash) e deployar **sem pedir confirmação** a cada PR. Padrão agora: abrir PR → CI `validate` verde → squash-merge → Vercel deploya. Não perguntar mais "posso mergear/deployar?". Travas mantidas: CI verde e revisão própria do diff quando toca pagamento/acesso. Registrado em [[Convenções de Trabalho]].
- **OSCE: erro na 1ª geração + lentidão; "Avaliando" duplicado na Prescrição (2026-06-16):** o gerador **vivo** de OSCE (`startSim` @~L8656 — o `@~L6411` é MORTO, mesma duplicação de `genFC`/`genMM`) gerava um caso rico (vinheta + labs + 4-6 passos com opções/feedback citando diretrizes) com `maxTokens=2600` → **truncava** o JSON → `parseJ` falha → "Resposta inválida". Como o `parseJ` já tira ```fences``` e extrai o `{...}`, falha de parse = **truncamento** (ou timeout da função). Correções: (a) `api/ai.js` `maxDuration:30` (já neste PR) evita timeout; (b) OSCE `maxTokens` 2600→**3200** (headroom) + `callAIJson` retries 0→1 (além do `simRetry`). Na **Prescrição Simulada**, o botão mostrava `ldHTML('Avaliando...')` **e** o painel `ldHTML('Avaliando prescrição...')` — "Avaliando" duplicado; removido o spinner do botão (fica só desabilitado), mantendo o loader informativo do painel. Reportado com prints do app em produção.
- **Geração por IA mais técnica e ancorada em diretrizes + grounding real por PubMed (2026-06-16):** os geradores de **questões, flashcards e mapas** usavam system prompts fracos ("Endocrinologista educador BR. JSON APENAS…"), sem diretrizes nem trava de linguagem. Criado o padrão único **`CLINICAL_AUTHORING`** + helper `authoringSys(formatoJSON)` no `index.html` (precisão técnica com valores/doses/critérios; ancoragem em diretrizes recentes NOMEADAS — SBD/SBEM/ADA/Endocrine Society/ATA/AACE/KDIGO + ano; PROÍBE "linguagem de IA"/clichês/hedging; PROÍBE inventar PMID/DOI). Aplicado a **todos os geradores vivos** dos 3 recursos: questões (`genQuestions`), flashcards (`genFC` @8537), mapas (`genMM` @8554) e os geradores **publicados pelo professor** (flashcards/mapas das Diretrizes + por tema). Atenção à duplicação: os `genFC`/`genMM` de ~L4500 são código MORTO (reatribuídos em ~L8530) — os vivos são os `callAIJson`. **Híbrido (escolha do usuário):** diretrizes nomeadas sempre (barato, toda geração) + **grounding REAL por PubMed onde mais importa = questões**. Novo módulo **`lib/pubmed.js`** (`pubmedGround(query)` → artigos recentes de revisão/diretriz/metanálise/ECR com PMID/ano/abstract; reusa pacing/retry/api-key do radar; **fail-open**: erro → `[]`, geração segue sem grounding). `api/ai.js` ganhou o campo **opcional** `grounding` (retrocompatível; injeta bloco FONTES com PMIDs reais no topo do prompt; `maxDuration:30` p/ headroom). O `grounding` é threaded por `callAI`/`callAIJson` (5º/6º arg) e passado pelos **3 recursos** (questões, flashcards e mapas — aluno e professor) usando o tema/subespecialidade como query; questões só com tema, mapas/flashcards sempre que há termo. **Só geração nova** (conteúdo de IA antigo intacto por ora). Sem nova função serverless (12/12; `lib/pubmed.js` é módulo). Validado: parsing/citações/fail-open por teste com fetch mockado (eutils é bloqueado no sandbox; caminho ao vivo é o mesmo já provado pelo radar em prod). Ver [[Banco de Questões]], [[Newsletter e Radar]].
- **Vaga de Sócio-fundador via PIX/boleto passou a contar no limite de 100 (2026-06-16):** verificação proativa do checkout antes do lançamento da campanha FUNDADOR achou um bug real: o webhook (`api/webhooks/pagarme.js`) liberava o Gold para pagamentos PIX/boleto mas **não gravava `notes`**, então a compra nunca era marcada `:fundador`. Como `lib/founder.js` conta as vagas por `notes ilike '*:fundador*'`, só o **cartão** (que `order.js` marca na hora) entrava no limite — com PIX dominante no Brasil, a oferta poderia furar muito as 100 vagas e o auto-desativar/contador de "vagas restantes" ficariam errados (o comprador via PIX recebia o Gold, mas fora da conta). **Fix:** o webhook lê `metadata.coupon` (que `order.js` já envia em `metadata`) e, quando é `FUNDADOR` no `plano:gold`, grava `notes='gold:anual:fundador'` — mesmo formato do cartão, idempotente com a corrida `order.paid`/`charge.paid`. Reaproveita `FOUNDER_COUPON`/`FOUNDER_PLAN` de `lib/founder.js` (fonte única). Distinto da corrida não-atômica já anotada (essa segue em [[Pendências]]). Sem nova função serverless (12/12). Ver [[Planos e Preços]], [[Pendências]].
- **Prescrição Comentada acessível ao professor + esclarecimento do gating presc/rx (2026-06-15):** confirmado que o gating já estava correto: **`rx` (Prescrição Comentada)** = Gold (`PANEL_MIN_TIER[rx]=2`) + professores (`canSeePanel` libera admin) + degustação 3 usos; **`presc` (Consultório/Memed)** oculto para alunos exceto `memed.teste@` (`prescAllowed`, em homologação — intencional). O Gold destrava de fato porque `endodirect_acessos_ativos()` emite o sentinela `'plano'` (supabase-setup.sql L377). **Única lacuna:** o professor não tinha botão para ABRIR a Prescrição Comentada (só Consultório). Adicionado item "💊 Prescrição Comentada" no menu do admin (`data-asec="rx"`) reaproveitando o MESMO `#panel-rx` do aluno (move/restaura igual ao Consultório: `__rxHome`/`restoreRxPanel`); os listeners (btn-rx-gen/eval/new, L9155-9157) são do startup e sobrevivem ao move. Ver [[Planos e Preços]], [[Arquitetura]].
- **Flashcards no painel do professor (publicados para todos os alunos) (2026-06-15):** nova seção "🃏 Flashcards" no admin (antes não existia), espelhando os Mapas Mentais: grade por subespecialidade → criar/editar/excluir cards, com **manual**, **Gerar com IA** (por tema) e **Importar arquivo** (PDF/TXT/imagem → IA gera vários cards). Nova coleção global **`fc_shared`** (`{id,front,back,sub,at}`), com todo o wiring: `globalStatePayload`, `applyStatePayload`, `GLOBAL_MERGE_KEYS`, `REMOTE_STATE_KEYS`, `persistFcShared`. **Entrega ao aluno:** (1) RPC `endodirect_member_content` ganhou a chave `fc_shared` (mesmo gating do `mm_shared`: plano OU degustação) — **aplicado no banco via SQL** + no `supabase-setup.sql`; (2) `seedFlashcards` semeia os cards de `fcShared` no baralho do aluno (`DB.fc`), idempotente por front + ledger `fc_shared_seeded` (respeita deleção; novos cards publicados entram na próxima carga). Reusa `aiFromFile`/`readFileForAI` da Parte 1. Parte 2 de 2. Ver [[Arquitetura]], [[Dados e Supabase]].
- **Mapas Mentais: importar arquivo (PDF/TXT/imagem) → IA gera/atualiza (2026-06-15):** no admin de Mapas Mentais, além de "Gerar com IA" por tema, agora dá para **importar um arquivo** e a IA monta o mapa, ou, editando um mapa, **atualizá-lo a partir de um arquivo**. Reusa a infra que já existia (`extractPdfText` via pdf.js; `documentBase64` no `/api/ai`). Novos helpers no cliente: `readFileForAI(file)` (PDF/TXT→texto; imagem→base64) e `aiFromFile(file,system,userPrefix)`. **Fix no `api/ai.js`:** imagem agora vai como bloco `type:'image'` (antes era sempre `'document'`, que a Anthropic rejeita para media_type de imagem) — necessário para o "PDF+TXT+imagem". Estrutura do mapa inalterada (`{root,branches:[{label,leaves}]}`), publica em `sharedMM`. Parte 1 de 2 (a 2 é a seção de Flashcards do professor). Ver [[Arquitetura]].
- **Classificação por IA no radar (subespecialidade + tipo canônicos) (2026-06-15):** antes a `subespecialidade` saía do **bucket da busca** (`sub.nome`) e o `tipo` do card era fixo `'Artigo'` (o cliente re-derivava por **regex** — fonte de vários bugs). Agora a `summarizeWithAI` (lib/radar.js) também pede `subespecialidade` (1 das 10) e `tipoMural` (1 de 7: Diretriz/Metanálise/Artigo de Revisão/Ensaio Clínico/Estudo Genético/Perspectiva Clínica/Estudo Original). `buildMuralItem` valida com `matchCanonical` (NFD/case-insensitive) e **só usa se casar com a lista**; senão **fallback** ao comportamento atual (bucket / `'Artigo'`). Os rótulos batem 1:1 com `MURAL_SUBSPECIALTY_FILTERS`/`MURAL_TYPES` do cliente (cross-check no PR), então o filtro do mural reconhece e o `normalizeMuralType` devolve verbatim. Vale só p/ itens NOVOS do radar; `studyType` (newsletter) intacto; sem mudança de custo relevante. Ver [[Newsletter e Radar]].
- **Alertas de falha por e-mail (2026-06-15):** antes só havia o healthcheck **semanal** (sempre mandava relatório) — uma falha do radar/newsletter só aparecia dias depois. Agora: (1) novo módulo `lib/alert.js` (`sendAlert(subject, lines)`, fail-safe, Resend → `endodirect_admins`/env `ALERT_TO`/fallback); (2) o cron `endocrine-radar` **dispara alerta na hora** se `runRadar` lançar (radar não atualizou) ou se a newsletter falhar; (3) o `healthcheck` virou **diário** (`vercel.json`: `0 11 * * 1`→`0 11 * * *`) e só envia quando há **falha** (qualquer dia) ou no **resumo de segunda** — sem ruído nos dias OK. Continua **2 crons** (limite Vercel) e `lib/alert.js` é módulo (não conta como função). Cobertura: "cron rodou e falhou" (alerta do radar) + "cron não rodou / radar parado" (healthcheck pega staleness). Ver [[Integrações]].
- **Aviso de "nova versão disponível" (anti-cache) (2026-06-15):** uma aba aberta há horas podia ficar presa em código antigo (o SW é network-first, mas sem navegação a aba não rebusca o HTML) — foi o que fez o professor ver edições "voltando sozinhas" antes do hard refresh. Adicionado bloco autocontido no topo do `index.html`: captura o **ETag/Last-Modified do `/` no load** e recompara via `HEAD {cache:'no-store'}` ao focar a aba (máx. 1x/min) e a cada 5 min; se mudou (= deploy novo), mostra um banner fixo "🔄 Nova versão disponível / Atualizar" que dá `location.reload()` (sem auto-reload; tem dispensar). Escolhido comparar o ETag do HTML (muda a CADA deploy) em vez do ciclo de update do SW (só dispara quando o `sw.js` muda — a maioria dos deploys mexe só no `index.html`). Autocontido (não depende do CSS/`notify`, que carregam depois). Ver [[Arquitetura]].
- **CI guard-rails (GitHub Actions) (2026-06-15):** novo `.github/workflows/ci.yml` + `scripts/ci-validate.js` (sem deps) rodam em cada PR/push p/ `main`: (1) `node --check` em `api/` e `lib/`, (2) `new Function` em cada `<script>` inline do `index.html`, (3) **falha se `api/` > 12 funções serverless** — o limite do plano Vercel Hobby que travou produção (deploy ERROR) em #311/#313. Objetivo: pegar antes do merge o que antes só se descobria em produção. Local: `node scripts/ci-validate.js`. Primeira de uma série de melhorias de robustez/inteligência pedidas pelo usuário (alertas de falha, aviso de nova versão anti-cache, classificação por IA no radar). Ver [[Convenções de Trabalho]].
- **Subespecialidades 'Reprodução' e 'Metabolismo' removidas do mural (2026-06-15):** apareciam no filtro/dropdown mas o radar **não as busca** (`SUBSPECIALTIES` em `lib/radar.js` tem só 10) → ficavam sempre vazias. Removidas de `MURAL_SUBSPECIALTY_FILTERS`. Como `inferMuralSubspecialty` ainda podia DERIVAR esses rótulos para itens sem `subespecialidade` (legados/Breaking News/manuais) — e aí o `muralFilterOptions` os re-adicionaria como chip dinâmico — também ajustei o classificador: `reproduc|fertil|gonad` → **Endocrinologia Feminina**; fallback final `'Metabolismo'` → **'Diabetes'** (bucket metabólico mais comum). Nenhum item GRAVADO usava os dois rótulos (conferido no banco), então sem migração SQL. Reprodução se sobrepõe a Feminina/Masculina; Metabolismo era só o catch-all. Ver [[Newsletter e Radar]].
- **Edição de card do radar revertia para o professor que ainda rodava código pré-#320 (cache) (2026-06-15):** sintoma "mudo a subespecialidade (ou traduzo o título) e volta sozinho para o valor antigo (Neuroendocrinologia / título em inglês)". Diagnóstico pelo banco: o `radar_avisos` guardava o valor ANTIGO e os saves do admin (mesmo `updated_by`) regravavam o antigo — exatamente o comportamento do `saveRemoteState` **pré-#320** (`p.radar_avisos = serverP.radar_avisos`, descarta a edição in-memory). O #320 já está READY em produção e a simulação Node do round-trip confirma que ele PERSISTE edições de cards não-semente; logo o navegador do professor estava servindo **HTML em cache** anterior ao #320 (o SW é network-first, mas o #320 não tem mudança visível, então não houve "gatilho" para o usuário recarregar de verdade). **Ação:** bump do cache do service worker (`endodirect-v2`→`v3`) para forçar atualização/limpeza do cache nos clientes + orientar hard refresh (Ctrl+Shift+R). Lição: quando o banco mostra o comportamento de uma versão antiga apesar do deploy novo estar READY, suspeitar de **cache do cliente** antes de procurar bug no código. **Títulos em inglês são por design** (o radar mantém o título no idioma original e traduz só o resumo). Ver [[Newsletter e Radar]].
- **Edição ainda revertia no F5 para os 6 cards-semente (subespecialidade/tipo) (2026-06-15):** mesmo após #320 (conserta o *save* do radar), os 6 artigos legados de `defaultMuralAvisos()` (sourceId em `defaultMuralMap`) continuavam revertendo a edição do professor no F5. Causa: `normalizeMuralAviso` re-aplicava o seed sobre o item gravado — `Object.assign({}, item, original, …)` — a CADA render/leitura, então `subespecialidade`/`tipo`/`texto` editados eram sobrescritos pelos valores hardcoded do seed (ex.: Notch volta a Tireoide/Estudo Genético; aldosterona a Adrenal). O save (#320) gravava a edição certa no banco, mas a normalização na LEITURA a desfazia → o professor via o valor antigo. Os 6 estão vivos no `radar_avisos`, então o bug era real para eles (o card da puberdade precoce do print NÃO é semente, por isso já persistia). **Fix:** invertida a precedência para `Object.assign({}, original, item, …)` — o seed só PREENCHE campos ausentes, o item gravado/editado sempre vence. Seguro porque o servidor já tem a cópia canônica desses itens (limpezas SQL #317/#319) e os strips defensivos cobrem resíduos de texto. Diagnóstico confirmado por simulação Node do round-trip (edit→save→F5) e pela consulta ao banco (a edição já estava gravada). Ver [[Newsletter e Radar]].
- **Edição de card do radar não persistia (subespecialidade/tipo voltavam no F5) (2026-06-15):** no `saveRemoteState` do admin, `radar_avisos` era **sobrescrito por inteiro** com a versão do servidor (`p[k]=serverP[k]`) — para não apagar itens novos do cron. Efeito colateral: qualquer edição do professor num card do radar (mudar subespecialidade/tipo/texto) era descartada no save → voltava no F5. Corrigido com **merge por chave estável** (`avisoKeyStr` = sourceId/link): base = `serverP.radar_avisos` (mantém adições do cron e a purga por TTL), mas aplica as edições in-memory (`radarAvisos`) nos itens de mesma chave. As chaves de newsletter seguem preservadas direto do servidor (admin não edita). Isso também faz o tipo escolhido manualmente (ver normalizeMuralType) finalmente "colar". Ver [[Newsletter e Radar]].
- **Exclusão de conteúdo global não persistia (voltava no F5) — merge com baseline (2026-06-15):** a proteção de concorrência (#305) usava `unionBy(servidor, local)`, que **preserva adições mas RESSUSCITA exclusões**: ao apagar um tema de Diretrizes (ou curso/podcast/prova) e salvar, o item ainda no servidor era re-unido e voltava no F5. Pior: bastava o **cron do radar** bumpar `updated_at` (sem 2º admin) para o save achar que "mudou" e re-unir. Corrigido com **merge baseado em baseline**: `captureGlobalBaseline()` guarda, junto com `lastGlobalUpdatedAt`, as chaves que a sessão tinha ao carregar/salvar (por coleção, via `GLOBAL_MERGE_KEYS`); `mergeConcurrent(servidor, local, baseline, keyFn)` parte do MEU estado (honra exclusões/edições) e só acrescenta itens do servidor cuja chave é **nova desde o baseline** (adições de outro editor/cron) — nunca os que eu apaguei. Removido o `unionBy`. Ver [[Dados e Supabase]].
- **Deploy de produção travado: limite de 12 serverless functions (Hobby) (2026-06-15):** o projeto está **no teto de 12 funções** em `api/` (todo READY mostra `lambdaRuntimeStats nodejs:12`). O #311 criou `api/cron/podcast-refresh.js` → **13ª função** → **deploy ERROR** (build conclui, falha em "Deploying outputs"). Isso travou em produção os PRs #311, #312 e #313 (o último READY no ar era o #310 — daí o botão "Enviar teste" reaparecendo e o bug do radar persistindo). 1ª tentativa (remover só o cron do `vercel.json`) **não bastou**: o arquivo continua contando como função. Correção definitiva: **excluído `api/cron/podcast-refresh.js`** (volta a 12). A atualização do feed agora roda **dentro do `endocrine-radar`** (diária, fail-safe), via o **módulo** `lib/podcasts.js` (módulos não contam como função). O limite de **2 cron jobs** também vale (radar + healthcheck). **Não criar novas funções em `api/` sem remover outra.** Ver [[Integrações]].
- **Estado por usuário não sobrescreve conteúdo global — `applyStatePayload(payload, personalOnly)` (2026-06-15):** o hydrate do `endodirect_app_state` (chaves pessoais) e do `endodirect_global_state` rodam no mesmo `Promise.all`; quando o app_state (com resíduo antigo de `radar_avisos`) resolvia depois, sobrescrevia os artigos novos do radar — bug "F5 atualiza e ~2s depois volta ao antigo". Fix: app_state hidrata com `personalOnly=true` (só `PERSONAL_STATE_KEYS`), nunca tocando conteúdo global. Ver [[Dados e Supabase]].
- **Botão "Enviar teste da newsletter" removido (2026-06-15):** a pedido. Endpoint `/api/newsletter/test` mantido (sem gatilho na UI). Ver [[Newsletter e Radar]].
- **Consultório (Memed) — visibilidade em homologação (2026-06-15):** OCULTO para alunos/médicos; aparece só (a) na conta de teste do Memed e (b) no **painel do professor**. Aluno: `prescAllowed()` (allowlist `MEMED_ALLOW`/fallback `memed.teste@`) gateia `canSeePanel('presc')` e o `sb-presc` (substitui `prescProfileOk`). Professor: nova seção `data-asec="consultorio"` que **move o mesmo `#panel-presc`** para dentro do `adm-main` (evita duplicar os IDs globais da ferramenta) e o devolve ao sair (`restorePrescPanel`, chamado também no `startApp`). Reabrir p/ médicos via `prescProfileOk` quando a Memed entrar em produção. Ver [[Pendências]].
- **Sync de `mm_shared` no preview/demo (2026-06-15):** a RPC `endodirect_public_content` (usada por quem **não** tem sessão Supabase — conta demo `alunopro`, logout, preview) **não** devolvia `mm_shared`, então o demo caía na cópia velha do `localStorage` (parecia "dessincronizado" do professor). Corrigido: a RPC passou a retornar `mm_shared` (igual a degustação já recebe via `endodirect_member_content`). Reforço no cliente: `applyStatePayload` agora grava `mm_shared` no `localStorage` ao aplicar do remoto (evita flash de dado velho antes do hydrate). Alunos reais nunca foram afetados (já vinham do `member_content`). Ver [[Dados e Supabase]].
- **Mapa "Diabetes Mellitus tipo 2" — critério TOTG-1h (2026-06-15):** corrigido o rótulo errado "TOTG 2h ≥209" → **"TOTG 1h ≥209 mg/dL (SBD/IDF)"** (209 é o limiar de 1 hora, não de 2h). Padronizado o texto idêntico no mapa publicado do professor (`mm_shared` no `endodirect_global_state`) e nas cópias seed dos alunos (`endodirect_app_state.payload.mm`), via SQL, **e** no `MM_SEED` (código). Alunos veem o mapa do professor ao vivo (sincronizado).
- **Mapas Mentais (admin) organizados por subespecialidade (2026-06-15):** a seção do professor passou a usar a **mesma grade de subespecialidades das Diretrizes** (taxonomia `DIR_SUBS` + `dirSubIcon`): nível 1 = grade de cards (com contagem de mapas), nível 2 = criar mapa + lista da subespecialidade, com Editar/Excluir. Substitui a lista plana com filtro. Estado `admMMSub`; helper `admMMSubList()`. O editor de mapa passou a usar `DIR_SUBS` (antes `ADM_MM_SUBESP`). Ver [[Arquitetura]].
- **App nativo (iOS/Android) — decisão: NÃO agora (2026-06-14/15).** Avaliado "vale a pena?": para o lançamento e os primeiros meses, **PWA + web bastam** (ícone na tela inicial sem custo/manutenção). Contra o nativo agora: **IAP da Apple** (15–30% sobre assinatura digital; pagar.me no navegador pode ser barrado), dupla esteira de release + revisão das lojas, risco de rejeição de "site embrulhado", e é cedo (pré-lançamento). **Revisitar** via **Capacitor** (embrulha a SPA; não React Native/Flutter) quando houver demanda/credibilidade ou push como retenção — resolvendo a estratégia de IAP antes. Opção de menor risco p/ "estar numa loja": PWA→Play Store via **TWA** (Android). Ver [[Pendências]].
- **PWA instalável (2026-06-14):** manifest + service worker **network-first** + ícones (logo dourada, alto contraste). Ver [[Arquitetura]].
- **Tema escuro como padrão (#250–#252):** a plataforma adota o visual escuro da landing (`data-theme` no `<html>`, default `dark`, reversível por usuário). Seletor Claro/Escuro no Perfil (aluno + professor) com **botões reais**. Ver [[Arquitetura]].
- **Videoaulas na landing (#244–#246):** card com os 4 professores (HLS via hls.js, grid 2×2); cortes começam no meio; a aula do Rodolpho (Vimeo) toca trecho fixo 00:55→01:05 em loop. Ver [[Arquitetura]].
- **Texto de apoio no Suporte (#248–#249):** mesma copy nos dois painéis (aluno + admin) antes do e-mail `contato@`.
- **Newsletter — priorização editorial (#275) + layout (#283):** ordena por tipo (revisão/metanálise/diretriz/consenso > ensaios/originais) e periódico (NEJM>Lancet>outros), data desc no desempate; e-mail em largura total, fontes maiores, logo no cabeçalho. Ver [[Newsletter e Radar]].
- **Importação de PDF lê texto no navegador (#276):** corrige HTTP 413 enviando texto extraído (pdf.js) em vez do binário. Ver [[Arquitetura]].
- **Navegação determinística (#284):** reload **mantém** a tela atual (professor na seção em que estava, aluno no painel); só **logout+login** volta ao padrão (Analytics / Mural). Substitui o comportamento anterior que forçava Analytics/Mural em todo reload. Ver [[Arquitetura]].
- **Diretrizes (renomeada de "Referência"):** seção subespecialidade→tema→diretriz com conteúdo híbrido (admin escreve resumo+bullets; IA deriva flashcards+mapa, tudo editável). Importação de PDF via IA. **Aluno vê em 3 formatos, só leitura**; no painel do professor, abrir um tema mostra a **mesma tela do aluno** + Editar/Excluir (#284). Gated por `DIRETRIZES_PUBLICADO=false` ("Em breve") até a curadoria liberar. Taxonomia de 11 subespecialidades (sem "Reprodução" → Endocrinologia Masculina).
- **IA do professor não consome créditos de aluno:** o assistente/geração das Diretrizes usa `/api/ai` (conta Anthropic do servidor), não as cotas do aluno.
- **Biblioteca pré-salva (perk de membro):** ~20 flashcards/subespecialidade (5 amostra p/ todos + 15 só membro) e 15 mapas mentais. Os originais **permanecem na degustação**; só os **novos** são member-only (`!isDegustacao()`; mapas novos com `member:true`). Médico deve revisar clinicamente os 180 flashcards gerados por IA — ver [[Pendências]].
- **Newsletter — priorização (lib/newsletter.js):** ordena por tipo (revisão/metanálise/diretriz/consenso > ensaios/originais) e, dentro disso, por periódico (NEJM > Lancet > outros), data desc como desempate. Layout em **largura total** (sem caixa centralizada), fontes maiores, logo do Endodirect no cabeçalho.
- **Calculadoras também no painel do professor (#277):** reaproveita as do aluno; funções escopadas a `calcRoot` para conviverem nos dois contêineres sem colisão de IDs.
- **Login inicial:** aluno sempre no **Mural** (`homePanel` Mural-first); professor no **Analytics** — mas só num login novo (ver navegação determinística acima).
- **Redirects de auth sempre canônicos (`www`):** `authBaseURL()` força confirmação/recuperação/OAuth para `https://www.endodirect.com.br` (origem só em localhost). Antes usava `window.location.origin`, então quem assinava pelo apex caía em `endodirect.com.br` (timeout) após confirmar o e-mail. O apex ainda precisa ser corrigido na Vercel/DNS — ver [[Pendências]].
- **Limite de 2 dispositivos por aluno** (anti-compartilhamento): tabela `endodirect_devices` + RPCs claim/check; heartbeat 60s; admins isentos. Ver [[Dados e Supabase]].
- **Login por usuário (anti-bleed):** o `user_profile`/estudo locais deixam de ser confiados cegamente. `doLogin` rastreia `last_uid`; ao trocar de conta no navegador, `clearLocalUserData()` limpa os dados locais do aluno anterior, e a decisão de onboarding passa a ser feita **após o hydrate** (perfil remoto manda) via `maybeOnboardAfterHydrate`. Corrige: aluno novo pulando o onboarding (CRM/UF) e herdando o perfil de um teste anterior. ⚠️ Editar o **override** de `doLogin` (~l.6583), não a base.
- **Confirmar e-mail = ON** no Supabase + auto-cadastro completo na tela de login: toggle Entrar/Criar conta, confirmar senha, painel persistente "confirme seu e-mail" e botão **Reenviar**. Ao clicar no link, o usuário volta logado (degustação → onboarding CRM/UF). Ver [[Pendências]].
- **CRM + UF obrigatórios no onboarding** (todos os perfis), para alimentar o Memed sem redigitar. Ver [[Integrações]].
- **Cofre de conhecimento no repo** (este cofre) em vez de exportação Obsidian para alunos. O Obsidian é para organizar a IA/projeto, não recurso de estudante. Botão de export removido (#172). Ver [[README]].
- **Calculadoras:** adicionar TmP/GFR (Payne) e escore-z de estatura/idade (OMS+CDC, LMS). OMS como referência padrão. Ver [[Calculadoras]].
- **Newsletter personalizada** por subespecialidade escolhida pelo aluno no Perfil.
- **pagar.me em produção (LIVE).** Detecção de modo LIVE corrigida (chaves v5 são `pk_`/`sk_` sem `_live_`).
- **Login com cadastro e-mail/senha** (além do Google), pois nem todos têm Gmail. Checkout reaproveita dados de cadastro/cobrança (cross-device).
- **Preços:** 2 pacotes (Standard/Gold), não 3. Anual em destaque. Ver [[Planos e Preços]].
- **Oferta de fundador** direcionada ao **Gold** (era Premium antes da simplificação para 2 pacotes); só no ciclo anual.
- **Assistente de Prescrição via API Memed** (substitui builder próprio). Ver [[Integrações]].
- **Automação semanal de health check** da plataforma.

## Regras permanentes
- **Nunca** armazenar dados de cartão (só tokenizar no pagar.me).
- **Nunca** colar segredos no chat, em commits ou neste cofre.
- Não reescrever commits já mergeados em `main`.
- Não incluir identificador de modelo em commits/PRs/artefatos.
